# Task Plan

## Linked Issue

GH-189

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP189-T1` Owner: lease-core | Depends on: approved spec | Done when: `repo_id` 只依赖 canonical common-dir 稳定 identity，跨 upstream/remote 配置变化不变；closed lease 不持久化 `status` 并含 nullable `remote_operation`；counter、non-prunable high-water witness、prepared allocation journal 与各自 parent 都逐段 no-follow 并做 regular/lstat-fstat identity 检查。fresh 初始化只能创建 token-0 counter+witness pair；无 journal 时二者 high-water 必须相等。acquire/resume/takeover 先 durable journal reservation，再按固定顺序推进 counter+witness；exact crash recovery 把已 reserved token 记为 skipped 并要求 fresh retry。normal release、resume replacement、audit prune 与后续失败后 witness 仍覆盖全部 token；单独 rollback/symlink/非普通文件/竞态替换均 fail closed。takeover 只在 allocation journal close 后计算 new digest/写 prepared audit；repo-wide non-blocking mutex、bounded TTL、boot+monotonic expiry、所有 parent fsync、barrier compare race、audit dirfd/recovery/256 retention 与 begin/end remote guard core 全有测试 | Verify: `python3 -m pytest -q tests/test_active_run_lease.py -k "identity or upstream or remote_change or status or mutex or counter or witness or allocation_journal or normal_release or audit_prune or rollback or skipped_token or allocate_before_audit or takeover or audit or symlink or recovery or retention or clock or remote_operation"` | Covers: B-001 B-002 B-004 B-006 B-008 B-009 B-010 B-011 B-014 B-015 B-017 | 新增 lease/counter/witness/allocation/audit core 与五个对应 closed schemas，不接 queue。
- [ ] `SP189-T2` Owner: runtime-binding | Depends on: SP189-T1 | Done when: `checkpoint_version: 4` 是唯一强制 `run_lease` repo/run/token 的 lease-aware 版本，lease 单向保存完整 checkpoint bytes digest，稳定绑定与任一侧篡改均有测试；首租/resume/takeover 过渡 lease 都是新 owner/new token 的 unbound state，写入同 identity v4 checkpoint 并立即 bind 前不能通过 resume/lane/remote-write；gate 只从 `--repo` 推导 canonical lease；Goal 只作连续性/预算上下文；v1–v3 fixture 继续离线兼容，两个未修改通用 tranche template 明确保持实际 `checkpoint_version: 2`，5+ 拒绝；只有 startup acquire 成功后使用 queue-only v4 templates。开始新增内容前把 active-run gate/rules 分别拆到 `checks/runtime_active_run_gate.py`、`checks/runtime_active_run_rules.py`，把 v4 schema 拆到 `schemas/runtime_checkpoint_v4.schema.json` 并由主 schema local `$ref`，把 `tests/test_specrail_schema.py` 的 review/runtime 区段迁到 `tests/test_review_runtime_schema.py`；所有 source/target 均 `<800` | Verify: `python3 -m pytest -q tests/test_runtime_ledger_gate.py tests/test_runtime_gate_rules.py tests/test_runtime_ledger_budget.py tests/test_runtime_ledger_queue.py tests/test_specrail_schema.py tests/test_review_runtime_schema.py tests/test_active_run_lease.py -k "lease or canonical or resume or takeover or rebind or goal or first_acquire or digest or version or budget or template" && rg -n '\"checkpoint_version\": 2' templates/tranche_checkpoint.md templates/zh-CN/tranche_checkpoint.md && rg -n run_lease templates/implx_checkpoint_v4.md templates/zh-CN/implx_checkpoint_v4.md && ! rg -n run_lease templates/tranche_checkpoint.md templates/zh-CN/tranche_checkpoint.md` | Covers: B-003 B-004 B-006 B-007 B-008 B-011 B-019 | 更新 split schema/rules/gate、v4 fixture/builder、版本接受测试和 queue-only v4 templates；通用模板与已声明 v1–v3 compatibility fixtures 保持不变。
- [ ] `SP189-T3` Owner: queue-integration | Depends on: SP189-T1 SP189-T2 SP189-T6 | Done when: queue 只通过 `python3 checks/active_run_lease.py --repo <repo> --json <operation>` 调用 inspect/acquire/renew/release/resume/takeover/recover/begin-remote/end-remote；base envelope 七键始终出现，operation/repo_id/lease_digest 按 spec 使用 JSON null，free/unsupported/unsafe/argument/schema error 的 closed JSON/nullability/退出码 0/2/3/4/5/64/70 全有测试；takeover 只传 opaque authorization ref，caller 不能注入 authorization/role-map path；每次 provider write 必须 begin guard 后同步执行，只有确定响应才 end，timeout/ambiguous 保留 `remote_operation_unknown` 并永久阻断 takeover，不能声称 provider-side fencing；startup v4、bounded TTL、new-owner bind、owner-only release、无 polling 与 review/auto fail-closed 语义保持。先把完整 lease 操作过程写入 `skills/specrail-implement-queue/references/active-run-lease.md`，SKILL 只保留必须读取 reference 的入口与 stop contract，二者均 `<800` | Verify: `python3 -m pytest -q tests/test_active_run_lease.py tests/test_active_run_takeover_authorization.py tests/test_runtime_ledger_gate.py -k "cli or json or nullability or argument_error or exit_code or redaction or queue or boundary or begin_remote or end_remote or provider_timeout or release or ttl or unsupported or recovery or takeover" && rg -n "references/active-run-lease.md" skills/specrail-implement-queue/SKILL.md` | Covers: B-003 B-004 B-005 B-006 B-007 B-010 B-011 B-013 B-014 B-015 B-016 B-018 | 更新 queue；若 GH-174 已合并则放入其 canonical runtime phase。
- [ ] `SP189-T4` Owner: pack-docs | Depends on: SP189-T3 | Done when: checker/schema required assets在 `checks/pack_asset_validation.py` exact 注册 fencing-counter/fencing-witness/fencing-allocation/lease/takeover-audit/takeover-authorization/takeover-consumption 七个 active-run closed schemas 与 `runtime_checkpoint_v4.schema.json`，把 `implx_checkpoint_v4.md` 注册进 `SPEC_TEMPLATE_FILES`（base + zh-CN localized parity 均受 `check_workflow.py` 检查），并在 `tests/test_pack_asset_validation.py` 更新 exact ownership 断言（含 template 集合）；`tests/test_active_run_schema.py` 独占七 schema 的 valid/malformed/unknown-field/binding 测试。AGENT_USAGE/CHANGELOG 与 Skill hash 同步，普通 workflow 不读取活动 counter/witness/journal/lease/audit/auth/remote-operation 状态 | Verify: `python3 checks/check_workflow.py --repo . && python3 -m pytest -q tests/test_check_workflow.py tests/test_pack_asset_validation.py tests/test_active_run_schema.py tests/test_specrail_schema.py tests/test_review_runtime_schema.py -k "active_run or required or ownership or checkpoint_v4"` | Covers: B-012 B-013 B-016 B-017 B-018 | 完成 pack wiring。
- [ ] `SP189-T6` Owner: takeover-authorization | Depends on: SP189-T1 | Done when: 新 adapter/authorization schema 以 host-configured verifier 解析 opaque authorization ref，caller 不能选择 adapter 或注入 authorization/role-map 文件；adapter 只接受 current-conversation evidence + maintainer role map，exact 绑定 authorization ID/decision `takeover_once`/repo/old digest-run-token/new run-owner/reason digest/actor/source/conversation evidence/freshness；adapter unavailable、请求者自报 marker、错 role、错绑、过期、auto/merge auth 替代全部失败。consumption 使用独立 closed `active_run_takeover_consumption.schema.json`；parent 从 canonical fd 逐段 `O_NOFOLLOW|O_DIRECTORY` 打开并做 lstat/fstat identity，final 以 dirfd-relative `O_EXCL|O_NOFOLLOW` exact mode/single-link 创建，malformed/错绑/重放与 parent/file symlink/swap 均 fail closed 且不触碰外部 sentinel。takeover 后续失败仍 consumed 且需新授权 | Verify: `python3 -m pytest -q tests/test_active_run_takeover_authorization.py tests/test_active_run_lease.py -k "authorization or maintainer or role_map or exact_binding or unavailable or expired or replay or consumption or parent_swap or external_sentinel or malformed or takeover_once"` | Covers: B-005 B-006 B-008 B-016 | 新增 host-owned external authorization adapter、authorization+consumption schemas/tests；不得从 lease CLI 生成授权。

## 并行拆分

- `T1 → (T2 ∥ T6) → T3 → T4`：T1 独占 lease core、counter/witness/allocation/lease/audit
  schemas 与 core tests；T2 独占 runtime gate/rules/checkpoint schema 的 source+split targets、
  `tests/test_specrail_schema.py`/`tests/test_review_runtime_schema.py` 及 runtime tests；T6 独占
  takeover authorization adapter、authorization/consumption schemas 与其测试。三者不得
  共享 writable file；T3 在两条接口固定后串行接 queue，并独占 queue SKILL/reference。
- T4 只在 T1/T2/T3/T6 均完成后串行注册 pack ownership、更新 docs/hash 与
  `tests/test_active_run_schema.py`，不得反向修改 core/split contract。
- 只读 reviewer 可并行，不得修改 manifest 文件。

## 验证

- [ ] `SP189-T5` Owner: verification-owner | Depends on: SP189-T1 SP189-T2 SP189-T3 SP189-T4 SP189-T6 | Done when: focused/full/pack/depth/diff/hash 全绿；不同 upstream 的 worktrees/进程及 remote 变化证明唯一 owner；counter/witness/allocation/lease/audit/consumption canonical no-follow identity、normal-release/resume/audit-prune 后 high-water rollback detection、prepared allocation recovery 与 token-before-audit/skipped gap、resume/takeover rotation+unbound rebind、external exact one-time authorization、consumption parent swap/external sentinel、CLI closed JSON/nullability/exit/redaction、通用实际 v2 与 queue-only v4 template 选择、remote begin/end 与 ambiguous-call takeover exclusion、replace race、fsync failure、prepared recovery/retention、monotonic expiry、review+auto unsupported、bounded TTL 全有 exact-head evidence；manifest 40 个 unique planned paths，五个 split targets 都存在，动态 line-count gate 证明所有 planned text assets `<800`；无 GH-160 diff | Verify: `python3 -m pytest -q tests/test_active_run_lease.py tests/test_active_run_takeover_authorization.py tests/test_active_run_schema.py tests/test_runtime_ledger_gate.py tests/test_check_workflow.py tests/test_pack_asset_validation.py tests/test_specrail_schema.py tests/test_review_runtime_schema.py && python3 -m pytest -q && python3 checks/check_workflow.py --repo . --all-specs && python3 tools/spec_depth_audit.py --spec-dir specs/GH189 --gate && git diff --check && test -f checks/runtime_active_run_gate.py && test -f checks/runtime_active_run_rules.py && test -f schemas/runtime_checkpoint_v4.schema.json && test -f skills/specrail-implement-queue/references/active-run-lease.md && test -f tests/test_review_runtime_schema.py`；再从 tech manifest 动态解析每个 planned path 并断言 `test $(wc -l < "$path") -lt 800` | Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 | exact-head 交付证据。

## Handoff Notes

- 当前只允许 write_spec；spec 合并并转 ready_to_implement 前不得实现。
- 不得自动 takeover/kill/删除他人 lease，remote write 仍遵守当前会话授权；任何
  `remote_operation_unknown` 都没有 force-clear/takeover escape hatch。
- manifest 完整列出 40 个 unique planned paths、v4 hard-coded consumers、五个 U-16 split
  targets、builder/acceptance tests 与新增 v4 fixture；所有 planned text assets 必须 `<800`；
  通用 tranche templates 保持实际 v2，已有 v1–v3 fixtures 按兼容合同保持不变；queue-only
  v4 templates 只有成功 acquire 后才能使用，不含 GH-160。
