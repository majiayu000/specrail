# Task Plan

## Linked Issue

GH-180

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP180-T1` 在 `checks/check_workflow.py` 增加纯 artifact-presence 的 `invalid | staged | complete` shape helper 和稳定 packet snapshot sha256，并让 validator 接受有效 product/tech-only packet；同步修改 standalone `evaluate.py`，使缺 tasks 成为 staged/optional 而非 `spec.tasks_present` failure，tasks 存在时两个入口都继续执行全部 task 内容校验。缺失、空白、不可读或错误 identity 的 product/tech 仍 fail closed。更新 `tests/test_evaluate.py` 与 `tests/test_check_workflow_paths.py`，覆盖 staged、complete、invalid、present-invalid tasks、symlink/identity、半写文件、重复运行与 snapshot 改变后摘要变化。Covers: B-001 B-002 B-004 B-005 B-011 B-014 B-015 B-019。Owner: validator lane。Depends on: none；开始前须确认本文件有效，禁止生产代码编辑先于有效 task plan。Done when: 两个 evaluator 对 staged/complete 语义一致，坏 tasks 不降级，失败与中断均重新全量校验。Verify: `/usr/bin/python3 -m pytest -q tests/test_evaluate.py tests/test_check_workflow_paths.py`。
- [ ] `SP180-T2` 在 `tests/test_check_workflow.py` 增加显式 packet 与 `--all-specs` CLI 审计回归，驱动 `checks/check_workflow.py` 对成功和失败 packet 按稳定路径顺序输出 `shape=<staged|complete|invalid>; readiness=unproven; packet_snapshot_sha256=<digest>`，保留既有最终消息与退出码。Covers: B-001 B-002 B-003 B-004 B-005 B-010 B-014 B-015 B-018 B-019。Owner: CLI audit lane。Depends on: SP180-T1。Done when: CLI 报告 shape、artifact、snapshot 与失败原因，但不把 shape 表述为授权；相同 tree 重跑输出一致。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py`。
- [ ] `SP180-T3P` 落地 runtime-provider、grant registry selector 与 portable verifier client boundary。新增 `checks/runtime_invocation_provider.py` adapter、`checks/runtime_invocation_context.py` closed-envelope/binding validator、`schemas/runtime_invocation_context.schema.json`、`integrations/runtime-invocation-provider.md`、`tests/test_runtime_invocation_provider.py` 与 `tests/test_runtime_invocation_context.py`。runtime owner 独占 fixed authenticated IPC、session/current-generation registry、用户授权写入且 caller 不可写的 grant registry、issuer/private keys、OS trust store，以及 guaranteed `specrail.runtime.invocation-verifier.v1` RFC8785-JCS/Ed25519 verifier；repo client 不依赖未声明的 Python crypto/JCS package。caller record 只提供 `grant_id` 与 invocation/repository id+name/issue/route selector；provider 必须读取并验证含同一 immutable repository identity 的 runtime-owned active grant 后才签发 response，`authorization_grant_sha256` 的 canonical grant、request/response 与 stable cross-challenge digest 都必须绑定 repository id+name。stable digest 另绑定 invocation id/generation/issue/route/auth_mode/runtime grant id+digest/waived gates，排除 request/challenge/timestamps/signature 与 `key_id`；每个 fresh response 的 active/non-revoked key 和签名仍由 verifier 独立校验，允许两个重叠 active key 间合法 rotation。Covers: B-008 B-009 B-014 B-015 B-018。Owner: SpecRail provider-integration lane（repo client）；External owner/prerequisite: runtime owner 的 provider/grant registry/verifier/issuer/trust-store deployment。Depends on: SP180-T1。Done when: valid repository-bound runtime grant 正例通过；missing/wrong/rename-drift/cross-repository identity、caller 自造/unknown/revoked/expired/mismatched grant、provider/verifier unavailable、untrusted peer、endpoint/verifier/root 注入、unknown/not-yet-valid/expired/revoked key、non-Ed25519/non-JCS、bad/extra fields、future/expired response、challenge/request replay 与 generation change均 fail closed；两次 challenge 使用不同 active key仍通过 stable binding。Verify: `/usr/bin/python3 -m pytest -q tests/test_runtime_invocation_provider.py tests/test_runtime_invocation_context.py`。
- [ ] `SP180-T3` 收紧完整 evidence gate，同时忠实保留现有 auth policy。review lifecycle 必须是同一 issue 的 `ready_to_spec → spec_pr_open → spec_review → spec_approved`；same-repository approval source 绑定 immutable repository id、PR number、head repository/ref/SHA、完整 changed paths，maintainer approval review 的 commit OID 精确等于该 head，且被接受 review 的 `submittedAt` 必须严格早于 `spec_approved` event；多条 exact-head APPROVED review 按 `(submittedAt, review_id)` 稳定选择符合权限且早于该 event 的最新一条，先打 label 后补 review 以 `approval_review_after_lifecycle` 拒绝。从该 SHA 的 product/tech blobs 生成 `approved_spec_snapshot_sha256`。accepted `ready_to_implement` event id/time 必须严格晚于 `spec_approved`（auto 时晚于 runtime grant `authorized_at`），且 event actor 必须独立通过同一 maintainer permission lookup，缺失/查询失败/低权限 fail closed。duplicate collector 先从 repo-owned `workflow.yaml` artifact templates 与 closed role table 派生 `product_spec | tech_spec | task_plan | packet_evidence` allowlist；manifest `spec_refs` 必须是无重复子集、包含 product+tech，并与 implementation `paths` disjoint，caller/manifest 不得扩大 role/path。duplicate gate 只排除 changed paths 全部属于 validated `spec_refs` 的 exact approved spec PR 及 exact-head branch；实现文件伪装 spec ref、其它 PR/branch 或 source identity/head/path/query 漂移仍阻断。route 用 `spec_snapshot_sha256` 比较 approval；issue/duplicate evidence 增加 repository id、必填 UTC `collected_at`、envelope audit hash 与只排除 `collected_at` 的 semantic snapshot hash；完整 `packet_snapshot_sha256` 只用于 saved-result/artifact drift。`schemas/evaluation_result.schema.json` 和 route result 增加闭合 `authorization_scope`：staged 只输出 `task_planning`/`plan_tasks` 并阻断 implement，complete 才可输出 `production_implementation`/`implement`；`--verify-result` 强制 `--consume-for` exact match，production 必拒 staged/legacy/missing-scope result。`workflow.yaml` 只读；auto 由 SP180-T3P 查询 repository-bound runtime grant并 fresh challenge。consumer 用 fresh issue/duplicate evidence、同一 selector、新 challenge 与 verifier 重验全部 binding；合法 `collected_at`/`key_id` 变化不算 drift，其它 repository/grant/generation/readiness/spec-source/spec/packet/PR/branch/scope 变化均拒绝。同步 schema、fixtures/support 与 route/duplicate tests。Covers: B-006 B-007 B-008 B-009 B-013 B-014 B-015 B-018 B-019。Owner: evidence gate lane。Depends on: SP180-T1 SP180-T3P。Done when: deterministic exact-source duplicate exemption、readiness actor authority、staged/production result scope、cross-repository replay、exact-head review timestamp ordering、label-before-review rejection、closed allowlist/disjointness、post-approval readiness、semantic-vs-envelope、spec-vs-packet 与 runtime-grant/rotation-stable binding 正负例齐全，所有修改文件 `<800`。Verify: `/usr/bin/python3 -m pytest -q tests/test_issue_evidence_freshness.py tests/test_route_gate.py tests/test_github_issue_evidence.py tests/test_github_issue_route_evidence.py tests/test_configured_spec_path_review_regressions.py tests/test_route_gate_sensitive.py tests/test_duplicate_work_gate.py tests/test_github_duplicate_evidence.py tests/test_specrail_schema.py`。
- [ ] `SP180-T4` 更新 `AGENT_USAGE.md`、`PLAN.md`、`README.md`、双语 PR template 与七个 workflow/entrypoint skill，统一 staged lifecycle、exact-head spec-only duplicate exemption、readiness actor authority、repository-bound auto grant、task-planning/production scope、semantic/envelope 与 spec/packet snapshot 合同。`PLAN.md` 必须移除“非 sensitive 无 approval timeline”及“sensitive 只能 merged default-branch PR 批准”的冲突：所有 review/default implement 都使用 open same-repository exact-head APPROVED spec PR lifecycle，sensitive registry 只叠加额外约束。write_spec 只写 product/tech；review/default implement 等待同仓 spec PR exact-head maintainer approval；auto 用 repository-bound caller selector 查询 runtime-owned grant 并通过 provider/verifier fresh challenge-response。staged allowed result 只能创建 tasks；tasks 有效后重跑 route，并以 `--verify-result <result> --consume-for production_implementation --evidence <fresh-evidence> --duplicate-evidence <fresh-duplicate-evidence>`（auto 重供 selector）通过才可启动生产代码 lane。文档必须声明 provider + grant registry + verifier 是 auto availability prerequisite；任一失败只阻断 auto，可显式走 review 但不得 silent downgrade。记录纠偏、漂移后重采与 GH-180 `partial_unproven` bootstrap；删减重复文字确保 queue skill `<800`。Covers: B-003 B-006 B-007 B-008 B-009 B-010 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019。Owner: contract docs lane。Depends on: SP180-T1 SP180-T3P SP180-T3。Done when: PLAN 与正常 lifecycle 一致；禁止 endpoint/verifier/root/context 注入、standing/persisted waiver、caller 自造/跨仓 grant、staged result 启动生产、旧 selector/context/result 自证 current、旧 semantic/spec/packet snapshot、saved hash 自比较或 bootstrap 扩权。Verify: `! rg -n -- "--(state|label) ready_to_(spec|implement)" PLAN.md README.md AGENT_USAGE.md skills`；`rg -n "auth_mode|waiv|repository_id|runtime.invocation|grant|verifier|challenge|staged|task_planning|production_implementation|consume-for|needs_tasks|spec_snapshot_sha256|packet_snapshot_sha256|verify-result|duplicate-evidence|fresh" PLAN.md README.md AGENT_USAGE.md skills/implx/SKILL.md skills/specrail-workflow/SKILL.md skills/specrail-write-product-spec/SKILL.md skills/specrail-write-tech-spec/SKILL.md skills/specrail-plan-tasks/SKILL.md skills/specrail-implement/SKILL.md skills/specrail-implement-queue/SKILL.md templates/pull_request.md templates/zh-CN/pull_request.md`；再运行 `test $(wc -l < skills/specrail-implement-queue/SKILL.md) -lt 800`。
- [ ] `SP180-T5` 仅更新 `skills-lock.json` 中 SP180-T4 七个已修改 skill 的 sha256，不改变 skill 集合、顺序、路径或其它 hash。Covers: none。Owner: skill-lock lane。Depends on: SP180-T4。Done when: `implx`、workflow、两个 focused write、plan-tasks、direct implement、implement-queue 的 hash 与内容一致，其他 lock diff 为空。Verify: `/usr/bin/python3 checks/check_workflow.py --repo .` 并复核 `git diff -- skills-lock.json`。
- [ ] `SP180-T6` 执行 focused、standalone evaluator、CLI、issue/exact-head approval/readiness actor/runtime grant-provider-verifier/duplicate evidence、result schema/scope、shared runtime-ledger mapping、PLAN/entrypoint docs/templates、fixture migration、queue、full、all-spec、depth、diff 与 size 验证。review 正例必须有同仓 exact-head approval、`APPROVED submittedAt < spec_approved < ready_to_implement`、authorized readiness actor，且 duplicate gate 只排除 deterministic closed allowlist 中、与 implementation paths disjoint 的 exact spec-only source；auto 正例必须有 repository-bound runtime-owned active grant + fresh authenticated provider/verifier response；两者都须 fresh trusted readiness、fresh duplicate semantic snapshot、valid complete packet、`production_implementation` scope 与全部 digest match。负例覆盖 label-before-review、实现文件注入 `spec_refs`、source PR/head/path/query 漂移或其它 duplicate、readiness actor 缺失/低权限/查询失败、`spec_approved` 误映射 `needs_tasks`、staged/legacy/missing/wrong scope production consumption、timestamp-only recapture 不误拒、semantic drift、tasks 只改变 packet snapshot、caller 自造/跨仓 grant、provider/verifier unavailable、untrusted peer、endpoint/verifier/root 注入、invalid/revoked key/grant、non-Ed25519/non-JCS、challenge replay、旧 generation/selector/response/result 跨 repository/invocation 重放、auto waive 其它 gate；正例覆盖 `ready_to_implement` task-planning mapping 与两次 challenge 合法 key rotation。Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019。Owner: verification coordinator。Depends on: SP180-T1 SP180-T2 SP180-T3P SP180-T3 SP180-T4 SP180-T5。Done when: 所有 fresh 命令通过；manifest 唯一真相解析为 50 个 planned paths 且以该解析结果审计 scope，`workflow.yaml` 零 diff；host provider/grant-registry/verifier prerequisite 与双 owner 可判定；tracked bootstrap evidence 保持 `authorization_effect=none`。Verify: `/usr/bin/python3 -m pytest -q tests/test_evaluate.py tests/test_check_workflow_paths.py tests/test_check_workflow.py tests/test_issue_evidence_freshness.py tests/test_route_gate.py tests/test_runtime_invocation_context.py tests/test_runtime_invocation_provider.py tests/test_runtime_ledger_gate.py tests/test_github_issue_evidence.py tests/test_github_issue_route_evidence.py tests/test_configured_spec_path_review_regressions.py tests/test_route_gate_sensitive.py tests/test_duplicate_work_gate.py tests/test_github_duplicate_evidence.py tests/test_specrail_schema.py && /usr/bin/python3 -m pytest -q && /usr/bin/python3 checks/check_workflow.py --repo . --all-specs && /usr/bin/python3 tools/spec_depth_audit.py --spec-dir specs/GH180 --gate && git diff --check`，并从 tech manifest 动态读取 planned paths 执行 `<800` 行检查。

## 并行拆分

- Validator lane 独占写入 `checks/check_workflow.py`、`evaluate.py`、`tests/test_evaluate.py`、`tests/test_check_workflow_paths.py`。
- CLI audit lane 独占写入 `tests/test_check_workflow.py`；可在 SP180-T1 的 shape/output contract 稳定后并行补齐 CLI fixture，禁止修改 validator 文件。
- Provider-integration lane 在 SP180-T1 后独占写入 `checks/runtime_invocation_provider.py`、`checks/runtime_invocation_context.py`、`schemas/runtime_invocation_context.schema.json`、`integrations/runtime-invocation-provider.md`、`tests/test_runtime_invocation_provider.py`、`tests/test_runtime_invocation_context.py`；runtime owner 是外部 prerequisite，独占 provider/session+generation/grant registry/issuer/private keys/OS trust store/portable verifier，不写本 repo。
- Evidence gate lane 在 SP180-T3P 后独占写入 tech manifest 所列 issue/approval/duplicate evidence、route gate、label catalog、其余两个 evidence schema、`evaluation_result` schema、四个 fixture、test support 及对应 route/schema tests；不修改 provider-integration lane 文件，`workflow.yaml` 只读，auth-mode 行为由既有 policy 驱动。
- Contract docs lane 在 evidence contract 稳定后独占写入 `AGENT_USAGE.md`、`README.md`、双语 PR template 与七个 workflow/entrypoint skill。
- Skill-lock lane 仅在 SP180-T4 完成后独占写入 `skills-lock.json`。
- Reviewer lane 全程只读，复核 invariant 覆盖、planned-path 白名单、shape/readiness 措辞与生产代码 gate；不得与任何 lane 共享可写文件。

## 验证

- Product invariant 集与 task Covers 并集均为 `B-001..B-019`，无缺项。
- Planned changes 的唯一真相是 tech spec manifest；当前解析结果为 50 条路径。新增 `PLAN.md` 用于迁移冲突的 approval contract，`checks/specrail_lib.py` 与 `tests/test_runtime_ledger_gate.py` 用于精确修正并锁定 `needs_tasks → ready_to_implement`；既有 `schemas/evaluation_result.schema.json` 与 `tests/test_specrail_schema.py` 用于闭合 result scope/action 组合。scope/count gate 必须动态读取 manifest，不维护第二个手写数字。`workflow.yaml` 仅作为现有 auth policy 与 artifact-template classifier source，不修改、不加入 manifest。search-first 已确认 standalone evaluator、focused/queue skills、既有 approval/duplicate collectors 与 gate/schema/tests 都是真实入口，也确认 repo 没有可依赖的 portable crypto/JCS backend 或 route runtime grant/provider/verifier contract；因此新增的 adapter/context schema/integration/tests 只实现 runtime-owned portable verifier 边界，不引入未声明 Python dependency。已 851 行的 `tests/test_github_issue_evidence.py` 必须把 route/fixture 回归迁到已有 route-evidence test，两者修改后均 `<800`。
- staged packet 通过 artifact 校验但在 queue 中归类为 `needs_tasks`，route result 只授予 `task_planning`；只有 valid tasks + fresh complete result + production-scoped consumer gate 使 packet 成为生产实现候选，shape 或通用 allowed decision 本身始终不授予 readiness。
- 按 SP180-T6 的顺序产出本次 session 的 focused、full、workflow、depth、diff 与 size gate 新鲜证据。

## Handoff Notes

- Bootstrap 历史被诚实记录为 GH-180 一次性 direct-label auto exception：历史 waiver claim 与旧 implement gate decision 仅为 reported，invocation_id/route/exact waived_human_gates/exact implx-auto trigger/runtime anchor/grant 及原 issue-evidence `collected_at`/hash 均为 `unproven`；JSON 的 `authorization_effect=none`。实现时必须从当前 invocation 重新取得 runtime-owned active grant + verified live context，并重采 fresh issue/duplicate evidence；缺失、超窗或任一 binding 不匹配时停止。
- Host integration prerequisite：runtime owner 必须先部署固定 authenticated provider/verifier IPC、session/current-generation registry、user-issued grant registry、Ed25519 issuer 与 OS/runtime-owned active keyset。任一未部署时 SpecRail auto 明确 blocked，不得宣称 available；operator 只能显式选择正常 ordered human-lifecycle review route，不能 silent downgrade。
- 当前 task-planning 阶段必须把 GH-180 视为 `needs_tasks`；只消费 `task_planning` scope，本文件通过 validator 与 coverage gate 且 complete result 通过 `--consume-for production_implementation` 前禁止生产代码实现。
- 实现阶段只能修改 tech manifest 动态解析出的路径（当前 50 条）；四个 spec refs 不属于实现路径。
- B-012 的在途纠偏应在 GH-180 validator 落地后于原 PR/原分支执行；GH-180 的一次性 bootstrap 不是其它 packet 提前创建 tasks 的先例。
