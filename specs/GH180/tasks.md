# Task Plan

## Linked Issue

GH-180

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP180-T1` 在 `checks/check_workflow.py` 增加纯 artifact-presence 的 `invalid | staged | complete` shape helper 和稳定 packet snapshot sha256，并让 validator 接受有效 product/tech-only packet；同步修改 standalone `evaluate.py`，使缺 tasks 成为 staged/optional 而非 `spec.tasks_present` failure，tasks 存在时两个入口都继续执行全部 task 内容校验。缺失、空白、不可读或错误 identity 的 product/tech 仍 fail closed。更新 `tests/test_evaluate.py` 与 `tests/test_check_workflow_paths.py`，覆盖 staged、complete、invalid、present-invalid tasks、symlink/identity、半写文件、重复运行与 snapshot 改变后摘要变化。Covers: B-001 B-002 B-004 B-005 B-011 B-014 B-015 B-019。Owner: validator lane。Depends on: none；开始前须确认本文件有效，禁止生产代码编辑先于有效 task plan。Done when: 两个 evaluator 对 staged/complete 语义一致，坏 tasks 不降级，失败与中断均重新全量校验。Verify: `/usr/bin/python3 -m pytest -q tests/test_evaluate.py tests/test_check_workflow_paths.py`。
- [ ] `SP180-T2` 在 `tests/test_check_workflow.py` 增加显式 packet 与 `--all-specs` CLI 审计回归，驱动 `checks/check_workflow.py` 对成功和失败 packet 按稳定路径顺序输出 `shape=<staged|complete|invalid>; readiness=unproven; packet_snapshot_sha256=<digest>`，保留既有最终消息与退出码。Covers: B-001 B-002 B-003 B-004 B-005 B-010 B-014 B-015 B-018 B-019。Owner: CLI audit lane。Depends on: SP180-T1。Done when: CLI 报告 shape、artifact、snapshot 与失败原因，但不把 shape 表述为授权；相同 tree 重跑输出一致。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py`。
- [ ] `SP180-T3P` 落地 runtime-provider client boundary。新增 `checks/runtime_invocation_provider.py` adapter、`checks/runtime_invocation_context.py` validator、`schemas/runtime_invocation_context.schema.json`、`integrations/runtime-invocation-provider.md`、`tests/test_runtime_invocation_provider.py` 与 `tests/test_runtime_invocation_context.py`。integration contract 固定 logical local IPC service `specrail.runtime.invocation.v1`，由 OS/runtime package 解析并验证 peer identity；CLI/env/repo/record/result 均无 endpoint/provider/root 参数。adapter 每次用 CSPRNG 生成独立 32-byte challenge + 16-byte request id；provider 在 authenticated transport session 上查询 current invocation/generation，返回绑定 challenge、invocation、issue、route、authorization JCS-SHA256 与精确 waived gates 的闭合响应。签名固定为 Ed25519，bytes 固定为 `SpecRail-Runtime-Invocation-v1\0` + 去除 signature 后的 RFC8785-JCS UTF-8；trust root 只从 OS/runtime-owned `specrail.runtime.invocation.v1` trust-store keyset 取得并强制 not_before/not_after/revoked_at。Covers: B-008 B-009 B-014 B-015 B-018。Owner: SpecRail provider-integration lane（repo client）；External owner/prerequisite: runtime owner 独占 service、session/generation registry、issuer/private keys、OS trust-store deployment/rotation/revocation。Depends on: SP180-T1。Done when: valid provider 正例通过；provider unavailable、untrusted peer、endpoint/root 注入、unknown/not-yet-valid/expired/revoked key、non-Ed25519/non-JCS、bad/extra fields、future/expired response、challenge/request mismatch/replay、record/context mismatch 与 generation change 均 fail closed；provider 未部署时 auto 报明确 blocked，正常 review route 可显式使用但不得 silent downgrade 或声称 auto available。Verify: `/usr/bin/python3 -m pytest -q tests/test_runtime_invocation_provider.py tests/test_runtime_invocation_context.py`。
- [ ] `SP180-T3` 收紧完整 evidence gate，同时忠实保留现有 auth policy。`checks/github_issue_evidence.py` 与 `schemas/issue_evidence.schema.json` 增加必填 UTC `collected_at`，并为 review/default implement 提供闭合的 `spec_lifecycle_approval`；`checks/github_approved_spec_evidence.py` 复用既有 timeline/default-base/permission 查询，验证同一 issue 上按序的 `spec_pr_open → spec_review → spec_approved`，且 approval actor 满足 maintainer policy。`labels.yaml` 声明三个 lifecycle labels。`workflow.yaml` 保持只读且不进入 manifest：`checks/route_gate.py` 默认 review 并强制 lifecycle；auto 接收 `--auth-mode auto --auth-evidence <record>` 后必须内部调用 SP180-T3P adapter，route 与 `--verify-result` 各发新的 challenge，调用方不能重供 signed context。任一参数单独出现或 readiness label 均不足；route result 绑定 authorization kind、authorization/context sha256 与 invocation generation。两种模式都强制随后取得的 fresh trusted readiness，拒绝 CLI `--state ready_to_*` 与 `--label ready_to_spec|ready_to_implement`。同时在 duplicate schema/collector/gate 增加 UTC/freshness 与 canonical snapshot；result 绑定 issue、authorization/context、duplicate、artifact 与 packet hashes。consumer 必须用 `--verify-result`、fresh issue、fresh duplicate evidence，并在 auto 时重供同一 record、由 adapter fresh challenge 重验全部 binding。更新 fixtures、support 与 route/duplicate tests。Covers: B-006 B-007 B-008 B-009 B-013 B-014 B-015 B-018 B-019。Owner: evidence gate lane。Depends on: SP180-T1 SP180-T3P。Done when: review/default 验证 ordered human lifecycle；auto 仅在 record + provider fresh response 严格匹配时 waive `spec_approval`；两者都验证 fresh trusted readiness、fresh duplicate、packet 与 saved-result binding；provider failure、caller/record/saved result 自证 current、旧 response/record/result 跨 invocation replay、generation/证据/PR/branch/摘要漂移均拒绝；迁移不丢测试且所有修改文件 `<800`。Verify: `/usr/bin/python3 -m pytest -q tests/test_issue_evidence_freshness.py tests/test_route_gate.py tests/test_github_issue_evidence.py tests/test_github_issue_route_evidence.py tests/test_configured_spec_path_review_regressions.py tests/test_route_gate_sensitive.py tests/test_duplicate_work_gate.py tests/test_github_duplicate_evidence.py`。
- [ ] `SP180-T4` 更新 `AGENT_USAGE.md`、`README.md`、双语 PR template 与七个 workflow/entrypoint skill，统一 staged lifecycle、auth mode 与 snapshot-bound gate：write_spec 只写 product/tech；review/default implement 等待 ordered human lifecycle approval；auto 必须让 scoped authorization record 与 provider fresh challenge-response 匹配，且仍真实设置并重采 readiness、fresh duplicate evidence。tasks 有效后重跑 route，并以 `--verify-result <result> --evidence <fresh-evidence> --duplicate-evidence <fresh-duplicate-evidence>`（auto 重供 record，adapter 内部发新 challenge）通过才可启动生产代码 lane。文档必须声明 host provider deployment 是 auto availability prerequisite；provider 失败只阻断 auto，可显式走正常 review route但不得 silent downgrade。记录纠偏、漂移后重采与 GH-180 `partial_unproven` bootstrap；删减重复文字确保当前 799 行 queue skill 修改后仍 `<800`。Covers: B-003 B-006 B-007 B-008 B-009 B-010 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019。Owner: contract docs lane。Depends on: SP180-T1 SP180-T3P SP180-T3。Done when: 公开入口忠实保留 workflow auto waiver，但禁止 endpoint/root/context CLI/env/repo 注入、standing/persisted/跨 invocation waiver、caller/record/saved result 自证 current、shape/readiness label 独立授权、旧 issue/authorization/context/duplicate/packet snapshot、saved hash 自比较或 bootstrap 扩权。Verify: `! rg -n -- "--(state|label) ready_to_(spec|implement)" README.md AGENT_USAGE.md skills`；`rg -n "auth_mode|waiv|runtime.invocation|provider|challenge|staged|needs_tasks|packet_snapshot_sha256|verify-result|duplicate-evidence|fresh" README.md AGENT_USAGE.md skills/implx/SKILL.md skills/specrail-workflow/SKILL.md skills/specrail-write-product-spec/SKILL.md skills/specrail-write-tech-spec/SKILL.md skills/specrail-plan-tasks/SKILL.md skills/specrail-implement/SKILL.md skills/specrail-implement-queue/SKILL.md templates/pull_request.md templates/zh-CN/pull_request.md`；再运行 `test $(wc -l < skills/specrail-implement-queue/SKILL.md) -lt 800`。
- [ ] `SP180-T5` 仅更新 `skills-lock.json` 中 SP180-T4 七个已修改 skill 的 sha256，不改变 skill 集合、顺序、路径或其它 hash。Covers: none。Owner: skill-lock lane。Depends on: SP180-T4。Done when: `implx`、workflow、两个 focused write、plan-tasks、direct implement、implement-queue 的 hash 与内容一致，其他 lock diff 为空。Verify: `/usr/bin/python3 checks/check_workflow.py --repo .` 并复核 `git diff -- skills-lock.json`。
- [ ] `SP180-T6` 执行 focused、standalone evaluator、CLI、issue/approval-or-waiver/runtime-provider/duplicate evidence、fixture migration、entrypoint docs/templates、queue、full、all-spec、depth、diff 与 size 验证；review/default 正例必须有 ordered human lifecycle，auto 正例必须有严格匹配的 record + fresh authenticated challenge-response；两者都须 fresh trusted readiness、fresh duplicate、valid complete packet 与全部 digest match，并为 provider unavailable/untrusted peer、endpoint/root 注入、unknown/invalid/revoked key、non-Ed25519/non-JCS、challenge replay、旧 generation/record/response/saved result 跨 invocation重放、auto 试图 waive 其它 gate 建负例。Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019。Owner: verification coordinator。Depends on: SP180-T1 SP180-T2 SP180-T3P SP180-T3 SP180-T4 SP180-T5。Done when: 所有 fresh 命令通过；四十三个 planned paths 外无实现 diff，`workflow.yaml` 零 diff；host prerequisite/双 owner 在 integration contract 与 public docs 可判定；tracked bootstrap evidence 保持 direct transition、reported decisions/waiver claim 与 unproven invocation_id/route/exact waived_human_gates/exact implx-auto trigger/runtime anchor/issue evidence 分栏，无 `invocation_scoped: true` 或 `spec_approval_waived...: true` 语义，`authorization_effect=none`。Verify: `/usr/bin/python3 -m pytest -q tests/test_evaluate.py tests/test_check_workflow_paths.py tests/test_check_workflow.py tests/test_issue_evidence_freshness.py tests/test_route_gate.py tests/test_runtime_invocation_context.py tests/test_runtime_invocation_provider.py tests/test_github_issue_evidence.py tests/test_github_issue_route_evidence.py tests/test_configured_spec_path_review_regressions.py tests/test_route_gate_sensitive.py tests/test_duplicate_work_gate.py tests/test_github_duplicate_evidence.py && /usr/bin/python3 -m pytest -q && /usr/bin/python3 checks/check_workflow.py --repo . --all-specs && /usr/bin/python3 tools/spec_depth_audit.py --spec-dir specs/GH180 --gate && git diff --check`，并对四十三个 planned paths 执行 `<800` 行检查。

## 并行拆分

- Validator lane 独占写入 `checks/check_workflow.py`、`evaluate.py`、`tests/test_evaluate.py`、`tests/test_check_workflow_paths.py`。
- CLI audit lane 独占写入 `tests/test_check_workflow.py`；可在 SP180-T1 的 shape/output contract 稳定后并行补齐 CLI fixture，禁止修改 validator 文件。
- Provider-integration lane 在 SP180-T1 后独占写入 `checks/runtime_invocation_provider.py`、`checks/runtime_invocation_context.py`、`schemas/runtime_invocation_context.schema.json`、`integrations/runtime-invocation-provider.md`、`tests/test_runtime_invocation_provider.py`、`tests/test_runtime_invocation_context.py`；runtime owner 是外部 prerequisite，只拥有 server/session registry/issuer/private keys/OS trust store，不写本 repo。
- Evidence gate lane 在 SP180-T3P 后独占写入 tech manifest 所列 issue/approval/duplicate evidence、route gate、label catalog、其余两个 evidence schema、四个 fixture、test support 及对应 tests；不修改 provider-integration lane 文件，`workflow.yaml` 只读，auth-mode 行为由既有 policy 驱动。
- Contract docs lane 在 evidence contract 稳定后独占写入 `AGENT_USAGE.md`、`README.md`、双语 PR template 与七个 workflow/entrypoint skill。
- Skill-lock lane 仅在 SP180-T4 完成后独占写入 `skills-lock.json`。
- Reviewer lane 全程只读，复核 invariant 覆盖、planned-path 白名单、shape/readiness 措辞与生产代码 gate；不得与任何 lane 共享可写文件。

## 验证

- Product invariant 集与 task Covers 并集均为 `B-001..B-019`，无缺项。
- Planned changes 恰好是 tech spec manifest 中的四十三条路径；`workflow.yaml` 仅作为现有 auth policy source，不修改、不加入 manifest。search-first 已确认 standalone evaluator、focused/queue skills、既有 approval collector 与 duplicate collector/gate/schema/tests 都是真实入口，也确认没有可复用的 route current-invocation provider adapter 或 host integration contract；因此新增的 provider adapter、context verifier/schema、integration contract 与两份 focused tests 构成窄化 trust boundary。已 851 行的 `tests/test_github_issue_evidence.py` 必须把 route/fixture 回归迁到已有 route-evidence test，两者修改后均 `<800`。
- staged packet 通过 artifact 校验但在 queue 中归类为 `needs_tasks`；只有 valid tasks 使 packet 成为生产实现候选，shape 本身始终不授予 readiness。
- 按 SP180-T6 的顺序产出本次 session 的 focused、full、workflow、depth、diff 与 size gate 新鲜证据。

## Handoff Notes

- Bootstrap 历史被诚实记录为 GH-180 一次性 direct-label auto exception：历史 waiver claim 与旧 implement gate decision 仅为 reported，invocation_id/route/exact waived_human_gates/exact implx-auto trigger/runtime anchor 及原 issue-evidence `collected_at`/hash 均为 `unproven`；JSON 的 `authorization_effect=none`。实现时必须从当前 invocation 重新建立 authorization record + runtime-owned live current-invocation context，并重采 fresh issue/duplicate evidence；缺失、超窗或任一 binding 不匹配时停止。
- Host integration prerequisite：runtime owner 必须先部署固定 authenticated IPC、session/current-generation registry、Ed25519 issuer 与 OS/runtime-owned active keyset。未部署时 SpecRail auto 明确 blocked，不得宣称 available；operator 只能显式选择正常 ordered human-lifecycle review route，不能 silent downgrade。
- 当前 task-planning 阶段必须把 GH-180 视为 `needs_tasks`；本文件通过 validator 与 coverage gate 前禁止生产代码实现。
- 实现阶段只能修改 tech manifest 中的四十三条路径；四个 spec refs 不属于实现路径。
- B-012 的在途纠偏应在 GH-180 validator 落地后于原 PR/原分支执行；GH-180 的一次性 bootstrap 不是其它 packet 提前创建 tasks 的先例。
