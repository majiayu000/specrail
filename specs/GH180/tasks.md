# Task Plan

## Linked Issue

GH-180

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP180-T1` 在 `checks/check_workflow.py` 增加纯 artifact-presence 的 `invalid | staged | complete` shape helper 和稳定 packet snapshot sha256，并让 validator 接受有效 product/tech-only packet；同步修改 standalone `evaluate.py`，使缺 tasks 成为 staged/optional 而非 `spec.tasks_present` failure，tasks 存在时两个入口都继续执行全部 task 内容校验。缺失、空白、不可读或错误 identity 的 product/tech 仍 fail closed。更新 `tests/test_evaluate.py` 与 `tests/test_check_workflow_paths.py`，覆盖 staged、complete、invalid、present-invalid tasks、symlink/identity、半写文件、重复运行与 snapshot 改变后摘要变化。Covers: B-001 B-002 B-004 B-005 B-011 B-014 B-015 B-019。Owner: validator lane。Depends on: none；开始前须确认本文件有效，spec-first 生产代码编辑不得先于有效 task plan，direct/mixed 由 SP180-T3 的 route-kind 合同判定。Done when: 两个 evaluator 对 staged/complete 语义一致，坏 tasks 不降级，失败与中断均重新全量校验。Verify: `/usr/bin/python3 -m pytest -q tests/test_evaluate.py tests/test_check_workflow_paths.py`。
- [ ] `SP180-T2` 在 `tests/test_check_workflow.py` 增加显式 packet 与 `--all-specs` CLI 审计回归，驱动 `checks/check_workflow.py` 对成功和失败 packet 按稳定路径顺序输出 `shape=<staged|complete|invalid>; readiness=unproven; packet_snapshot_sha256=<digest>`，保留既有最终消息与退出码。Covers: B-001 B-002 B-003 B-004 B-005 B-010 B-014 B-015 B-018 B-019。Owner: CLI audit lane。Depends on: SP180-T1。Done when: CLI 报告 shape、artifact、snapshot 与失败原因，但不把 shape 表述为授权；相同 tree 重跑输出一致。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py`。
- [ ] `SP180-T3P` 落地 runtime-provider、grant registry selector 与 portable verifier client boundary。新增 `checks/runtime_invocation_provider.py` adapter、`checks/runtime_invocation_context.py` closed-envelope/binding validator、`schemas/runtime_invocation_context.schema.json`、`integrations/runtime-invocation-provider.md`、`tests/test_runtime_invocation_provider.py` 与 `tests/test_runtime_invocation_context.py`。runtime owner 独占 fixed authenticated IPC、session/current-generation registry、用户授权写入且 caller 不可写的 grant registry、issuer/private keys、OS trust store，以及 guaranteed `specrail.runtime.invocation-verifier.v1` RFC8785-JCS/Ed25519 verifier；repo client 不依赖未声明的 Python crypto/JCS package。caller record 只提供 `grant_id` 与 invocation/repository id+name/issue/route selector；provider 必须读取并验证含同一 immutable repository identity 的 runtime-owned active grant 后才签发 response，`authorization_grant_sha256` 的 canonical grant、request/response 与 stable cross-challenge digest 都必须绑定 repository id+name。stable digest 另绑定 invocation id/generation/issue/route/auth_mode/runtime grant id+digest/waived gates，排除 request/challenge/timestamps/signature 与 `key_id`；每个 fresh response 的 active/non-revoked key 和签名仍由 verifier 独立校验，允许两个重叠 active key 间合法 rotation。Covers: B-008 B-009 B-014 B-015 B-018。Owner: SpecRail provider-integration lane（repo client）；External owner/prerequisite: runtime owner 的 provider/grant registry/verifier/issuer/trust-store deployment。Depends on: SP180-T1。Done when: valid repository-bound runtime grant 正例通过；missing/wrong/rename-drift/cross-repository identity、caller 自造/unknown/revoked/expired/mismatched grant、provider/verifier unavailable、untrusted peer、endpoint/verifier/root 注入、unknown/not-yet-valid/expired/revoked key、non-Ed25519/non-JCS、bad/extra fields、future/expired response、challenge/request replay 与 generation change均 fail closed；两次 challenge 使用不同 active key仍通过 stable binding。Verify: `/usr/bin/python3 -m pytest -q tests/test_runtime_invocation_provider.py tests/test_runtime_invocation_context.py`。
- [ ] `SP180-T3` 收紧完整 evidence gate，同时忠实保留现有 auth、route 与 sensitive policy。先从 trusted transition、packet/PR relation、queue-derived tier 确定闭集 `implementation_entry_kind: spec_first | direct_bug | mixed_impl`：spec-first 才要求同一 issue 的 `ready_to_spec → spec_pr_open → spec_review → spec_approved` 与独立 exact-head spec PR；direct-bug 保留 `SPEC.md` accepted-small-bug + `triaged → ready_to_implement` 且无 spec packet；standard/fastlane mixed-impl 保留 single-PR relation，不要求更早 spec-only PR，也不产生 `approved_spec_pr_exemption`；caller 自报、冲突或歧义 fail closed。spec-first approval source 绑定 immutable repository id、PR number、head repository/ref/SHA、完整 changed paths，maintainer approval review 的 commit OID 精确等于该 head，且 `submittedAt < spec_approved < ready_to_implement`；readiness actor 独立通过 maintainer lookup。从 exact SHA blobs 生成 `approved_spec_snapshot_sha256`。duplicate collector 的通用 classifier 只从 `workflow.yaml` templates 派生 `product_spec | tech_spec | task_plan | invalid`；不得硬编码 bootstrap/packet-evidence role。仅 GH180 exact issue/path 且 JSON 的 version/issue、顶层/observed/verdict 三个 `authorization_effect:none` 与 `partial_unproven` 全匹配时，consumer overlay 返回无授权效果的 `gh180_bootstrap_audit`；复制/改名/提权内容失败。manifest `spec_refs` 必须无重复、含 product+tech、与 implementation `paths` disjoint；duplicate gate 只排除 exact approved spec source。sensitive spec-first 的 open PR 仅证明 lifecycle，production 仍须 exact revision merged to trusted default base，并通过 existing `approved_spec` 的 `merged_at`/`merge_commit_sha`/ancestry；direct/mixed sensitive 也不绕过。route 用 spec/packet snapshots、fresh envelope/semantic evidence 和闭合 `authorization_scope` 区分 staged task-planning 与 complete production；consumer 重验 fresh evidence、selector、challenge 与全部 binding。同步 schema、fixtures/support 与 route/duplicate/sensitive tests。Covers: B-006 B-007 B-008 B-009 B-013 B-014 B-015 B-018 B-019 B-020 B-021 B-022。Owner: evidence gate lane。Depends on: SP180-T1 SP180-T3P。Done when: route-kind compatibility、GH180-only overlay、exact-source exemption、readiness actor、scope、review ordering、semantic/snapshot/runtime binding 及 sensitive open-negative/merged-positive/ancestry-drift 正负例齐全，所有修改文件 `<800`。Verify: `/usr/bin/python3 -m pytest -q tests/test_issue_evidence_freshness.py tests/test_route_gate.py tests/test_github_issue_evidence.py tests/test_github_issue_route_evidence.py tests/test_configured_spec_path_review_regressions.py tests/test_route_gate_sensitive.py tests/test_duplicate_work_gate.py tests/test_github_duplicate_evidence.py tests/test_specrail_schema.py`。
- [ ] `SP180-T4` 更新 `AGENT_USAGE.md`、`PLAN.md`、`README.md`、双语 PR template 与七个 workflow/entrypoint skill，统一 staged lifecycle、route-kind、exact-head duplicate exemption、readiness actor、repository-bound auto grant、task-planning/production scope与 snapshots。`PLAN.md` 为 spec-first review/default 补齐 open same-repository exact-head APPROVED lifecycle，但保留 accepted-small-bug direct route、standard/fastlane single-PR mixed route及 sensitive exact revision merged-base/ancestry production gate；不得把 staged lifecycle 泛化到所有 implement，也不得用 open sensitive PR 取代现有 enforcement。write_spec 只写 product/tech；auto 使用 runtime-owned grant/provider/verifier；staged result 只能创建 tasks，complete 后以 fresh production consumer gate 才能启动代码 lane。文档声明 host prerequisites、GH180-only `partial_unproven` overlay、fail-closed 与无 silent downgrade；删减重复文字确保 queue skill `<800`。Covers: B-003 B-006 B-007 B-008 B-009 B-010 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 B-020 B-021 B-022。Owner: contract docs lane。Depends on: SP180-T1 SP180-T3P SP180-T3。Done when: PLAN 同时与 spec-first lifecycle、direct/mixed compatibility、sensitive merged-base enforcement 一致；禁止 route/bootstrap 自报提权、endpoint/verifier/root/context 注入、standing/persisted waiver、caller 自造/跨仓 grant、staged result 启动生产、旧 selector/context/result/snapshot 自证 current。Verify: `! rg -n -- "--(state|label) ready_to_(spec|implement)" PLAN.md README.md AGENT_USAGE.md skills`；`rg -n "auth_mode|waiv|repository_id|runtime.invocation|grant|verifier|challenge|staged|task_planning|production_implementation|consume-for|needs_tasks|spec_snapshot_sha256|packet_snapshot_sha256|verify-result|duplicate-evidence|fresh|direct_bug|mixed_impl|merged_at|merge_commit_sha" PLAN.md README.md AGENT_USAGE.md skills/implx/SKILL.md skills/specrail-workflow/SKILL.md skills/specrail-write-product-spec/SKILL.md skills/specrail-write-tech-spec/SKILL.md skills/specrail-plan-tasks/SKILL.md skills/specrail-implement/SKILL.md skills/specrail-implement-queue/SKILL.md templates/pull_request.md templates/zh-CN/pull_request.md`；再运行 `test $(wc -l < skills/specrail-implement-queue/SKILL.md) -lt 800`。
- [ ] `SP180-T5` 仅更新 `skills-lock.json` 中 SP180-T4 七个已修改 skill 的 sha256，不改变 skill 集合、顺序、路径或其它 hash。Covers: none (仅同步已修改 skill 的 lock hash，不直接实现产品行为不变量)。Owner: skill-lock lane。Depends on: SP180-T4。Done when: `implx`、workflow、两个 focused write、plan-tasks、direct implement、implement-queue 的 hash 与内容一致，其他 lock diff 为空。Verify: `/usr/bin/python3 checks/check_workflow.py --repo .` 并复核 `git diff -- skills-lock.json`。
- [ ] `SP180-T6` 执行 focused、CLI、route/evidence/schema/runtime-ledger、docs/queue、full、all-spec、depth、diff 与 size 验证。spec-first review 正例必须有同仓 exact-head approval、`APPROVED submittedAt < spec_approved < ready_to_implement`、authorized readiness actor 与 exact-source duplicate exemption；auto 正例必须有 repository-bound runtime-owned active grant + fresh provider/verifier response。新增正负例必须覆盖：通用 classifier 不认识 bootstrap role；只有 GH180 exact-path/content overlay 成功且无 authorization effect；`spec_first|direct_bug|mixed_impl` 由 trusted evidence 唯一确定，accepted-small-bug direct 与 standard/fastlane mixed route 不被误拦；caller 自报/歧义 route 失败；sensitive open spec PR 单独失败，exact revision merged-base 正例通过，merge SHA/ancestry 漂移失败，direct/mixed sensitive 不绕过。既有负例继续覆盖 label-before-review、spec_refs 注入/source drift、readiness actor、scope misuse、semantic/artifact drift、runtime grant/provider/key/challenge/replay；正例继续覆盖 task-planning mapping 与合法 key rotation。Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 B-020 B-021 B-022。Owner: verification coordinator。Depends on: SP180-T1 SP180-T2 SP180-T3P SP180-T3 SP180-T4 SP180-T5。Done when: 所有 fresh 命令通过；实现改动的路径集合与 manifest 动态解析结果完全相等（无缺漏也无额外路径）、spec refs 仍为 4 条，`workflow.yaml` 的 diff 不超出 `implement` 条件 artifact 规则且 auth policy 段零 diff，existing sensitive enforcement 零 diff；tracked bootstrap evidence 保持 `authorization_effect=none`。Verify: `/usr/bin/python3 -m pytest -q tests/test_evaluate.py tests/test_check_workflow_paths.py tests/test_check_workflow.py tests/test_issue_evidence_freshness.py tests/test_route_gate.py tests/test_runtime_invocation_context.py tests/test_runtime_invocation_provider.py tests/test_runtime_ledger_gate.py tests/test_github_issue_evidence.py tests/test_github_issue_route_evidence.py tests/test_configured_spec_path_review_regressions.py tests/test_route_gate_sensitive.py tests/test_duplicate_work_gate.py tests/test_github_duplicate_evidence.py tests/test_specrail_schema.py && /usr/bin/python3 -m pytest -q && /usr/bin/python3 checks/check_workflow.py --repo . --all-specs && /usr/bin/python3 tools/spec_depth_audit.py --spec-dir specs/GH180 --gate && git diff --check`，并从 tech manifest 动态读取 planned paths 执行 `<800` 行检查。

## 并行拆分

- Validator lane 独占写入 `checks/check_workflow.py`、`evaluate.py`、`tests/test_evaluate.py`、`tests/test_check_workflow_paths.py`。
- CLI audit lane 独占写入 `tests/test_check_workflow.py`；可在 SP180-T1 的 shape/output contract 稳定后并行补齐 CLI fixture，禁止修改 validator 文件。
- Provider-integration lane 在 SP180-T1 后独占写入 `checks/runtime_invocation_provider.py`、`checks/runtime_invocation_context.py`、`schemas/runtime_invocation_context.schema.json`、`integrations/runtime-invocation-provider.md`、`tests/test_runtime_invocation_provider.py`、`tests/test_runtime_invocation_context.py`；runtime owner 是外部 prerequisite，独占 provider/session+generation/grant registry/issuer/private keys/OS trust store/portable verifier，不写本 repo。
- Evidence gate lane 在 SP180-T3P 后独占写入 tech manifest 所列 issue/approval/duplicate evidence、route gate、label catalog、其余两个 evidence schema、`evaluation_result` schema、四个 fixture、test support 及对应 route/schema tests；不修改 provider-integration lane 文件；`workflow.yaml` 只允许新增 `implement` 条件 artifact 规则，auth policy（`workflow.yaml:15-58`）逐字节不变，auth-mode 行为由既有 policy 驱动。
- Contract docs lane 在 evidence contract 稳定后独占写入 `AGENT_USAGE.md`、`README.md`、双语 PR template 与七个 workflow/entrypoint skill。
- Skill-lock lane 仅在 SP180-T4 完成后独占写入 `skills-lock.json`。
- Reviewer lane 全程只读，复核 invariant 覆盖、planned-path 白名单、shape/readiness 措辞与生产代码 gate；不得与任何 lane 共享可写文件。

## 验证

- Product invariant 集与 task Covers 并集均为 `B-001..B-022`，无缺项。
- Planned changes 的唯一真相是 tech spec manifest，路径数一律动态解析，不在 spec 里写死。新增 `PLAN.md` 用于迁移冲突的 approval contract，`checks/specrail_lib.py` 与 `tests/test_runtime_ledger_gate.py` 用于精确修正并锁定 `needs_tasks → ready_to_implement`；既有 `schemas/evaluation_result.schema.json` 与 `tests/test_specrail_schema.py` 用于闭合 result scope/action 组合。scope/count gate 必须动态读取 manifest，不维护第二个手写数字。`workflow.yaml` 已在 manifest 内，同时作为现有 auth policy 与 artifact-template classifier source；其 diff 严格限于 `tech.md` 第 3 节要求的 `implement` 条件 artifact 规则，auth policy（`workflow.yaml:15-58`）逐字节不变。search-first 已确认 standalone evaluator、focused/queue skills、既有 approval/duplicate collectors 与 gate/schema/tests 都是真实入口，也确认 repo 没有可依赖的 portable crypto/JCS backend 或 route runtime grant/provider/verifier contract；因此新增的 adapter/context schema/integration/tests 只实现 runtime-owned portable verifier 边界，不引入未声明 Python dependency。已 851 行的 `tests/test_github_issue_evidence.py` 必须把 route/fixture 回归迁到已有 route-evidence test，两者修改后均 `<800`。
- staged packet 通过 artifact 校验但在 queue 中归类为 `needs_tasks`，route result 只授予 `task_planning`；只有 valid tasks + fresh complete result + production-scoped consumer gate 使 packet 成为生产实现候选，shape 或通用 allowed decision 本身始终不授予 readiness。
- 按 SP180-T6 的顺序产出本次 session 的 focused、full、workflow、depth、diff 与 size gate 新鲜证据。

## Handoff Notes

- Bootstrap 历史被诚实记录为 GH-180 一次性 direct-label auto exception：历史 waiver claim 与旧 implement gate decision 仅为 reported，invocation_id/route/exact waived_human_gates/exact implx-auto trigger/runtime anchor/grant 及原 issue-evidence `collected_at`/hash 均为 `unproven`；JSON 的 `authorization_effect=none`。实现时必须从当前 invocation 重新取得 runtime-owned active grant + verified live context，并重采 fresh issue/duplicate evidence；缺失、超窗或任一 binding 不匹配时停止。
- 通用 classifier 只识别 workflow-derived product/tech/tasks；`bootstrap-evidence.json` 仅由 GH180 exact-path/content overlay 识别为无授权效果的 `gh180_bootstrap_audit`，禁止跨 issue 复用。
- staged lifecycle 只属于 `spec_first`。accepted-small-bug `direct_bug` 与 standard/fastlane `mixed_impl` 继续按现有 route gates 运行；sensitive 三类入口的生产实现都保留 existing merged-base/ancestry enforcement。
- Host integration prerequisite：runtime owner 必须先部署固定 authenticated provider/verifier IPC、session/current-generation registry、user-issued grant registry、Ed25519 issuer 与 OS/runtime-owned active keyset。任一未部署时 SpecRail auto 明确 blocked，不得宣称 available；operator 只能显式选择正常 ordered human-lifecycle review route，不能 silent downgrade。
- 本文件已存在且通过 repository validator，因此按 B-002 与 queue coverage 合同 GH-180 的 packet 状态是 `complete`，不是 `needs_tasks`——交接时必须如实记为**complete-but-not-authorized**，不得为了取得 `task_planning` scope 而误报 artifact 状态（该 scope 只对 staged packet 签发，误报会让 implementer 拿到无法消费的 result）。授权边界由 route 而非 artifact 状态决定：生产代码实现前必须取得针对当前 head 的 fresh production-scoped route 判定，并以 complete result 通过 `--consume-for production_implementation` 的 consumer 复验；两者任一缺失、过期或与当前 packet snapshot 不一致时禁止实现。
- 实现阶段只能修改 tech manifest 动态解析出的路径；四个 spec refs 不属于实现路径。
- B-012 的在途纠偏在 GH-180 validator 落地后由一个以 `main` 为 base 的独立 follow-up PR 执行（PR #179 已合并，原分支不再能改变默认分支）；本 PR 不交付该删除。GH-180 的一次性 bootstrap 不是其它 packet 提前创建 tasks 的先例。
