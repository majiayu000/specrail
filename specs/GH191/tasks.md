# Task Plan

## Linked Issue

GH-191

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP191-T1` Owner: ledger-gate | Depends on: SP191-T7 SP191-T8 | Done when: closed immutable-event ledger 分离 `attempt_started` 与唯一 terminal event，验证 scope/run/head 状态机、内部 hash chain、progress recompute；三阈值保持 GH-157 的 commit/commit/tranche 口径，覆盖 4↔5 commits、2↔3 same-fingerprint commits、2↔3 tranches，且含 2 attempts=3+2 commits 触发、5 attempts=4 commits 不触发、1 attempt=3 same commits 触发；逐 commit 只在可信 issue-reference provenance 为 true 时计数；公开 gate 强制消费 exact proof+reservation bytes 与受保护 runtime 的 canonical `attempt_started` candidate，并返回共享 `evaluation_result` exact closed projection；finalizer 只在 allowed action predicate 成立时签发 `authorized_action=open_issue_lane` 的 receipt；裸/non-allowed result 不开 lane，matching receipt 与 exact candidate 只能由 `append-start` 原子消费并落下 `attempt_started`、anchor 与 durable dispatch outbox，受保护 dispatcher 以稳定 `lane_dispatch_id` exactly-once 开 lane；finalize 后 crash/expiry/response loss 只能幂等 recover 或 cancel 并保持当前 action blocked | Verify: `python3 -m pytest -q tests/test_issue_progress_gate.py -k "ledger or event or threshold or commit_reference or evaluation_result or exact_keys or reservation or candidate or receipt or append_start or expiry or recover or outbox or dispatch"` | Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-011 B-012 B-016 B-017 B-019 B-021 B-022 B-023 B-024 B-025 | 新增 ledger schema/gate/template。
- [ ] `SP191-T2` Owner: collector | Depends on: SP191-T1 SP191-T7 SP191-T8 | Done when: bounded read-only collector 只接受已验证的 GitHub 与 runtime tranche-history envelopes，绑定 issue/head/run/spec IDs、签名 `provider_as_of`、adapter-run provenance、commit issue-reference derivation 与 snapshot digest，输出 start/terminal candidate，不信任 caller-authored evidence path/history、不读 session/raw logs、不读墙钟改变既有输入判断 | Verify: `python3 -m pytest -q tests/test_issue_attempt_collector.py tests/test_github_issue_attempt_evidence.py tests/test_runtime_issue_tranche_history_evidence.py -k "candidate or deterministic or as_of or provenance or commit_reference or tranche"` | Covers: B-001 B-002 B-003 B-004 B-009 B-012 B-016 B-019 B-023 B-024 | 新增 collector。
- [ ] `SP191-T3` Owner: queue-integration | Depends on: SP191-T6 SP191-T8 SP191-T9, GH-172/PR #186 merged into target base, GH-174/PR #192 rebased on GH-172 then merged, GH-189/PR #193 rebased on GH-174 then merged, GH-191 rebased on all three | Done when: repository overlay preflight 证明上述顺序且不把 open PR 当 merged；pre-lane gate 强制要求 exact proof+reservation+candidate 与 action-bound signed receipt；queue 只调用 writer `append-start` 完成 attempt/anchor/receipt/outbox 原子事务，再由受保护 dispatcher 幂等消费稳定 `lane_dispatch_id`，绝不凭 append outcome 自行 dispatch；blocked（trip、non-allowed/action mismatch、stale/unsigned result、reservation/finalize/append-start failure、pending anchor、untrusted GitHub/runtime history、scope authorization invalid、history loss 或其他 history 缺陷）无继续路径，remote park/draft 仅在已有授权时执行 | Verify: `python3 -m pytest -q tests/test_issue_attempt_writer.py tests/test_issue_progress_gate.py tests/test_repository_dependency_preflight.py -k "queue or authorization or anchor or reservation or candidate or receipt or append_start or outbox or dispatch or upstream or dependency"` | Covers: B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-013 B-014 B-015 B-017 B-018 B-019 B-020 B-021 B-022 B-023 B-024 B-025 B-026 | 只在 fresh truth 证明 upstream 按序合入后对齐最终 queue/run/fencing 合同。
- [ ] `SP191-T4` Owner: pack-docs | Depends on: SP191-T3 | Done when: assets/wiring/docs/hash 全部同步；ledger/anchor/current-proof/evaluation-reservation/decision-receipt/GitHub-evidence/runtime-history/scope-authorization/dependency-overlay schemas 全部注册进 `checks/pack_asset_validation.py` 的 `SPEC_SCHEMA_FILES` 并同步 exact ownership 断言；proof、reservation、receipt 三个独立 closed schema 的 required/unknown-field 与 cross-binding 正反测试完整；普通 consumer workflow 只做 generic asset/schema 校验，不读取 repo overlay、PR 编号或 GitHub | Verify: `python3 checks/check_workflow.py --repo . && python3 -m pytest -q tests/test_check_workflow.py tests/test_pack_asset_validation.py -k "schema or ownership or reservation or generic or consumer"` | Covers: B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 B-022 B-023 B-025 B-026 | pack 收口。
- [ ] `SP191-T6` Owner: writer-anchor | Depends on: SP191-T1 SP191-T2 SP191-T7 SP191-T8 | Done when: 唯一 writer helper 实现 `init-baseline`/`migrate-baseline`/start/terminal/scope/recover；用 expected ledger digest + 外部 anchor generation 做 prepare/CAS、temp+fsync+atomic replace+dir fsync、commit attestation；`begin-evaluation` 原子创建独占 reservation 并绑定 candidate digest、阻断普通 writer，`finalize-evaluation` 强制消费 exact reservation bytes 并对 generation/ledger/reservation/result/candidate digest 做 CAS，按 result action predicate 签发带 `authorized_action`/`expires_at` 的 receipt；只有 `authorized_action=open_issue_lane` 且携 matching receipt 与 exact candidate 的 `append-start` 可进入 provider transaction，原子完成 candidate 重验、receipt create-only consumption、ledger `attempt_started`、anchor commit 与唯一 durable dispatch outbox；受保护 dispatcher 以稳定 `lane_dispatch_id` 幂等 enqueue，下游 create-only，append response loss/retry/recover 不二次 dispatch；provider receipt state 跟踪 pending/consumed/cancelled/expired，prepared transaction 可幂等 recover，未 prepared 的过期/放弃 receipt 只能 cancel 留 tombstone、恢复 writer并阻断当前 action；finalize→append-start generation 前移、candidate/action 替换、过期/取消/重放/竞态均 fail closed；`open-scope` 验证 exact maintainer rescope/unpark 授权并原子消费；首次 migration 同时绑定完整 GitHub 与 runtime tranche-history digests，anchor 已存在但 ledger 缺失判 history loss | Verify: `python3 -m pytest -q tests/test_issue_attempt_writer.py tests/test_issue_progress_gate.py tests/test_github_issue_attempt_evidence.py tests/test_runtime_issue_tranche_history_evidence.py -k "writer or anchor or baseline or migration or recover or reservation or candidate or finalize or append_start or receipt or action or outbox or dispatch or expiry or cancel or scope_authorization"` | Covers: B-001 B-002 B-009 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 B-021 B-022 B-023 B-025 | 新增 writer、anchor/proof/reservation/receipt schemas/template 与测试。
- [ ] `SP191-T7` Owner: trusted-github-evidence-auth | Depends on: approved spec, SP191-T9 records fresh dependency status | Done when: 新增 `github_issue_attempt_evidence.py` 与 closed evidence/scope-authorization schemas；allowlisted issuer/trust root/adapter executable 对完整 GitHub query/pagination、repo/issue/head、`provider_as_of` 与 payload digest 签名；对每个 commit SHA 复用稳定 `references_issue_text()` 派生并签名 issue/message digest/predicate ID+version/boolean/derivation digest，prefix collision、错 SHA/issue 或 predicate/digest 漂移 fail closed；受保护 runtime 从 current queue/run/fencing 输入构造 canonical `attempt_started` candidate 并签名 provenance，再用 challenge 从 provider 同时获取 proof+reservation；loader 必须验证并传入两份 exact canonical bytes，candidate 字段替换失败；maintainer role map 产出 exact `open_scope_once` 授权，普通 writer/agent 不可伪造 | Verify: `python3 -m pytest -q tests/test_github_issue_attempt_evidence.py tests/test_issue_attempt_writer.py tests/test_issue_progress_gate.py -k "issuer or adapter or signature or pagination or freshness or challenge or reservation or candidate or scope_authorization or commit_reference or predicate"` | Covers: B-005 B-006 B-012 B-016 B-017 B-018 B-019 B-021 B-024 B-025 | 复用现有 trusted GitHub adapter/content-binding、稳定 issue-reference 谓词与一次性 review authorization 模式。
- [ ] `SP191-T8` Owner: trusted-runtime-history | Depends on: approved spec, SP191-T9 records fresh dependency status | Done when: 新增 `runtime_issue_tranche_history_evidence.py` 与 closed schema；只从受保护 checkpoint archive manifest 和显式 trusted git base/head 读取，逐 blob 运行 checkpoint schema + runtime gate，提取 exact issue/run/tranche/PR/head/start/terminal，证明 coverage window 无缺页/浅历史/空洞并签名；caller history、单一 current checkpoint、缺段、冲突/重复 tranche 或 source digest 不符均 `tranche_history_incomplete` | Verify: `python3 -m pytest -q tests/test_runtime_issue_tranche_history_evidence.py -k "archive or tracked or complete or incomplete or shallow or conflict or signature"` | Covers: B-007 B-009 B-012 B-015 B-016 B-019 B-023 B-025 | complete/incomplete fixtures 均为 schema-valid。
- [ ] `SP191-T9` Owner: repository-dependency-preflight | Depends on: approved spec | Done when: 新增 closed repo overlay、generic read-only helper 与 tests；overlay 独占 GH191 的 issue/PR 顺序和三组 exact planned-path overlaps，helper fresh 验证 default base、mergedAt/mergeCommit、ancestry、逐步 rebase 与 overlap 重算；通用 `check_workflow.py` 不含任何 #186/#192/#193 或网络查询；open/order-invalid/ready fixtures 分别产生 blocked/blocked/allowed | Verify: `python3 -m pytest -q tests/test_repository_dependency_preflight.py tests/test_check_workflow.py -k "overlay or dependency or order or rebase or overlap or generic or consumer"` | Covers: B-020 B-022 B-026 | 当前 fresh truth 三项仍 OPEN，故 implementation gate 预期 blocked。

## 并行拆分

- 外部固定串行 `GH-172/PR #186 merge → rebase+merge GH-174/PR #192 → rebase+merge GH-189/PR #193 → rebase GH-191`；截至 2026-07-26 fresh truth 三个 PR 均 OPEN，故当前不得开始实现。
- 内部固定串行 `T9 → T7 → T8 → T1 → T2 → T6 → T3 → T4 → T5`，evidence/provenance、ledger/fingerprint/writer/anchor/gate 是共享合同；保留已发布 T1..T7 ID，新增 runtime history 与 repository preflight 为 T8/T9。
- reviewer 可只读检查阈值误报，不修改 manifest。

## 验证

- [ ] `SP191-T5` Owner: verification-owner | Depends on: SP191-T1 SP191-T2 SP191-T3 SP191-T4 SP191-T6 SP191-T7 SP191-T8 SP191-T9 | Done when: focused/full/pack/depth/diff/hash 与三次 resume forward test 全绿；逐项映射固定为 `PR195-R2-F001 → B-021 → discussion_r3651623662 → issue-attempt-proof-generation-race.json`、`PR195-R2-F002 → B-022 → discussion_r3651623657 → issue-progress-evaluation-allowed.json + issue-progress-evaluation-blocked.json`、`PR195-R2-F003 → B-023 → discussion_r3651623659 → issue-attempt-tranche-history-complete.json + issue-attempt-tranche-history-incomplete.json`、`PR195-R2-F004 → B-024 → discussion_r3651623664 → issue-attempt-commit-reference-mixed.json`、`PR195-R2-F005 → B-025 → discussion_r3651623665 → current-state-proof/evaluation-reservation/decision-receipt schema ownership tests`、`PR195-R2-F006 → B-026 → discussion_r3651623666 → gh191-dependencies-open.json + gh191-dependencies-order-invalid.json + gh191-dependencies-ready.json`；额外 active hosted root `discussion_r3651554498 → B-018 → scope_authorization/unpark/replay tests`。验证 proof 签发后 generation 前移、finalize→append-start 前 generation 前移、receipt crash/abandon/expiry/replay/原子消费与 writer 恢复、完整 shared output、trusted history gap、issue-reference provenance、三个独立 closed schema ownership 与 repo-overlay isolation；不 resolve/reply threads，无 GH-160 diff | Verify: `python3 -m pytest -q tests/test_github_issue_attempt_evidence.py tests/test_runtime_issue_tranche_history_evidence.py tests/test_issue_attempt_collector.py tests/test_issue_attempt_writer.py tests/test_issue_progress_gate.py tests/test_repository_dependency_preflight.py tests/test_check_workflow.py tests/test_pack_asset_validation.py && python3 -m pytest -q && python3 checks/check_workflow.py --repo . --all-specs && python3 tools/spec_depth_audit.py --spec-dir specs/GH191 --gate && git diff --check` | Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 B-020 B-021 B-022 B-023 B-024 B-025 B-026 | exact-head 证据。
  该任务还必须固定验证 `PR207-R2-F001 → B-021 → append response loss/outbox exactly-once tests`、`discussion_r3652981107 → B-021/B-022 → non-allowed/action-mismatch receipt tests`、`discussion_r3652981111 → B-001/B-017/B-019/B-021 → candidate substitution tests`、`discussion_r3652981113 → B-017/B-025 → mandatory proof+reservation loader/schema tests`；证明 response loss 只产生一个 attempt/outbox/dispatch、non-allowed result 不追加 start、candidate 字段替换 fail closed，且 reservation exact bytes 是 loader/evaluator/finalizer 的必需输入。

## Handoff Notes

- 当前只允许 write_spec；合并/readiness gate 前不得实现。
- gate 永不执行 park/draft/comment；外部动作沿用用户授权边界。
- trusted anchor provider 位于 ledger/checkout 外；工作树内复制品不构成 B-013 证据。
- gate 的裸 evaluation result 不是 action authorization；provider reservation finalize
  只能从 closed result 派生 `authorized_action`；matching signed receipt 与 exact
  candidate 仍必须由 writer `append-start` 原子消费并落下 `attempt_started`、anchor 与
  durable dispatch outbox，再由受保护 dispatcher 以稳定 `lane_dispatch_id` exactly-once
  开 lane；append outcome 不是 dispatch 授权，过期/放弃 receipt 只恢复 writer。
- migration 同时要求 trusted GitHub 与 trusted runtime tranche-history envelopes；
  `current.json` 或 caller-authored history 不能替代 archive coverage。
- 截至 2026-07-26，PR #186/#192/#193 均 OPEN、`mergedAt=null`；状态只能由 implementation
  preflight 的 fresh GitHub evidence 更新，spec/PR body 不得宣称已合并。
- queue/lock 必须等待并按 `GH-172 → GH-174 → GH-189 → GH-191` 逐项 merge/rebase；
  任一步未完成都不能用复制未合并合同或直接改最终共享文件绕过。
- #186/#192/#193 只存在 repo dependency overlay；通用 checker 与 consumer pack 不得
  硬编码或查询这些编号。
