# Task Plan

## Linked Issue

GH-165

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP165-T1` 在 `checks/check_workflow.py` 实现并接入动态 `validate_skill_gate_availability(repo)`，同时在 `tests/test_check_workflow.py` 先补齐当前仓库/review 基线正例、动态 consumer、optional-only、逐组语义缺失、八 skill 全量稳定报错和 CLI 非零反例；候选仅由精确引用 `checks/route_gate.py` 的 `skills/*/SKILL.md` 发现，错误按相对路径排序并一次性返回。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018. Owner: validator lane（唯一 writable files：`checks/check_workflow.py`、`tests/test_check_workflow.py`）. Depends on: none. Done when: 验证器校验 Gate Availability 的 exists/decision、absent/stop、error/unavailable、限域专用人工授权、degraded evidence/稳定标记、禁止成功声明、并发重试中断恢复语义，拒绝 mandatory route gate 的 optional-only 入口，并在 `validate_skills_lock(repo)` 后进入既有聚合；正反例证明未来新增 consumer 无需 allowlist 即受约束，新增代码行覆盖率至少 80%。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py`，并检查 `git diff -- checks/check_workflow.py tests/test_check_workflow.py` 与覆盖率证据。
- [ ] `SP165-T2` 更新 `skills/specrail-workflow/SKILL.md` 的 route-specific Gate Availability 契约：选路与分派前先判定当前 gate，禁止把路由器结果替代叶子自检，完整定义缺失、运行错误、专用降级授权、审计标记、非法状态、并发/重试/中断与不可下游复用规则。Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-012 B-013 B-014 B-015 B-016 B-017. Owner: workflow-skill lane（唯一 writable file：`skills/specrail-workflow/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: 路由器不再以 optional-only 措辞进入 mandatory gate，只有当前调用真实 `allowed` 可作为通过；unavailable 默认停止，显式降级结果绑定 repository/route/task/run 且不能冒充 SpecRail 成功。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T3` 更新 `skills/specrail-implement/SKILL.md`，为 `implement` route 写入完整同构 Gate Availability 契约，并保留既有 `ready_to_implement`、非 `allowed` decision 与 rejection persistence 语义。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: implement-skill lane（唯一 writable file：`skills/specrail-implement/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: 直接调用实现 skill 也独立 fail closed；普通执行、`implx auto`、历史/推断授权无效，当前人工专用降级授权和 unavailable 输出完整且不扩张状态机权限。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T4` 更新 `skills/specrail-plan-tasks/SKILL.md`，为 `implement` route 写入完整同构 Gate Availability 契约，并保留任务覆盖检查和 rejection persistence。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: plan-tasks-skill lane（唯一 writable file：`skills/specrail-plan-tasks/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: 任务规划直接调用不能借用其他 skill 的 gate 结果；缺失/错误默认停止，授权降级带稳定标记与当前调用证据且不能满足后续 gate。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T5` 更新 `skills/specrail-write-product-spec/SKILL.md`，为 `write_spec` route 写入完整同构 Gate Availability 契约，并保留 `ready_to_spec`、产品规格边界与 rejection persistence。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: product-spec-skill lane（唯一 writable file：`skills/specrail-write-product-spec/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: gate 缺失或无有效 decision 时停止 SpecRail 规格 route；只有限域专用人工授权才可生成明确标识、不可充当通过证据的 degraded 产物。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T6` 更新 `skills/specrail-write-tech-spec/SKILL.md`，为 `write_spec` route 写入完整同构 Gate Availability 契约，并保留产品规格前置、planned-changes manifest 与 rejection persistence。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: tech-spec-skill lane（唯一 writable file：`skills/specrail-write-tech-spec/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: 技术规格直接调用按当前 repository/route/task/run 重新判定，缺失/错误/取消不留下成功结论，降级状态完整披露且不绕过 `write_spec` 状态转换。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T7` 更新 `skills/specrail-triage-issue/SKILL.md`，为 `triage_issue` route 写入完整同构 Gate Availability 契约，并把 unavailable 判定置于既有拒绝持久化流程之前。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: triage-skill lane（唯一 writable file：`skills/specrail-triage-issue/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: 分诊直接调用不会投机运行缺失命令或静默成功；拒绝、重试、降级和中断证据保持可区分且当前调用限域。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T8` 更新 `skills/specrail-diagnose-ci/SKILL.md`，为 `fix_ci` route 写入完整同构 Gate Availability 契约，并保留 `human_review`、根因诊断与 rejection persistence。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: diagnose-ci-skill lane（唯一 writable file：`skills/specrail-diagnose-ci/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: CI 诊断在 gate 不存在或执行失败时不会宣称 gated/verified/gate passed/merge-ready；专用降级授权和结果审计字段完整、限域且不可复用。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T9` 更新 `skills/specrail-release-note/SKILL.md`，为 `draft_release_note` route 写入完整同构 Gate Availability 契约，并保留 `merged` 状态、人类 release 权限与 rejection persistence。Covers: B-001 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017. Owner: release-note-skill lane（唯一 writable file：`skills/specrail-release-note/SKILL.md`）. Depends on: SP165-T1 validator contract. Done when: 发布说明直接调用独立判定 gate；缺失、错误、授权、审计与恢复路径完整，降级产物不成为 release 或其他后续 gate 的通过证据。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py -k gate_availability`。
- [ ] `SP165-T10` 在八个 skill 全部定稿后仅更新 `skills-lock.json` 中对应八项 `computedHash`，不得改变集合、路径、顺序或其他 skill 哈希。Covers: B-015 B-018. Owner: lock lane（唯一 writable file：`skills-lock.json`）. Depends on: SP165-T2 SP165-T3 SP165-T4 SP165-T5 SP165-T6 SP165-T7 SP165-T8 SP165-T9. Done when: 八个 SHA-256 与最终 `SKILL.md` 字节逐项一致，既有 lock 的路径/集合/排序校验通过且无实现 manifest 外写入。Verify: `/usr/bin/python3 checks/check_workflow.py --repo . --all-specs`，并用 `shasum -a 256` 逐项复核八个锁定文件。

## 并行拆分

- `SP165-T1` 必须先由 validator lane 串行完成，独占 `checks/check_workflow.py` 与 `tests/test_check_workflow.py`；其测试与语义标记是后续八个 skill lane 的共同只读契约。
- `SP165-T2`..`SP165-T9` 在 `SP165-T1` 完成后可分批并行；每个 lane 只写任务中声明的单个 `SKILL.md`，相互没有共享 writable file，也不得触碰 `skills-lock.json`。
- `SP165-T10` 等八个 skill lane 全部结束后串行执行，独占 `skills-lock.json`；计算哈希期间不再并行修改任何 skill。
- reviewer 与 verification owner 仅在 `SP165-T10` 后读取 exact-head diff、规格和命令输出；reviewer 只读、不写任何仓库文件。若发现问题，将 finding 退回对应文件 owner，修复完成后重新计算受影响哈希并重新开始 exact-head 验证。

## 验证

- [ ] `SP165-T11` 对 11 个 manifest 路径做 exact-head 只读复核，先运行 focused tests，再运行全量 pytest、workflow all-spec、GH165 depth gate、diff/文件规模与稳定标记搜索；确认 B-001..B-018 的任务覆盖与技术测试映射一致，且无未声明写入或被弱化的既有断言。Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018. Owner: verification owner + read-only reviewer（无 writable files）. Depends on: SP165-T1 SP165-T2 SP165-T3 SP165-T4 SP165-T5 SP165-T6 SP165-T7 SP165-T8 SP165-T9 SP165-T10. Done when: validator 的正反例、八 skill 同构/route-specific 契约、review 基线、lock 哈希、全量回归和规格深度全部通过；所有被修改文件 `<800` 行，reviewer 对 exact-head 给出无阻塞 finding 的只读结论。Verify: `/usr/bin/python3 -m pytest -q tests/test_check_workflow.py && /usr/bin/python3 -m pytest -q && /usr/bin/python3 checks/check_workflow.py --repo . --all-specs && /usr/bin/python3 tools/spec_depth_audit.py --spec-dir specs/GH165 --gate && git diff --check && rg -n "when available|## Gate Availability|SpecRail gate status: unavailable" skills/specrail-* && test "$(wc -l < checks/check_workflow.py)" -lt 800 && test "$(wc -l < tests/test_check_workflow.py)" -lt 800`。

## Handoff Notes

- 实现边界严格等于技术规格 manifest 的 11 个路径：`checks/check_workflow.py`、`tests/test_check_workflow.py`、八个目标 `SKILL.md` 与 `skills-lock.json`；不得新增共享 fragment、修改安装器/通用库或顺手修复其他 optional integration。
- 当前 GitHub readiness 仍为 `ready_to_spec`；规格 PR 合并后，协调者必须先把 issue 迁移到真实 `ready_to_implement` label，再采集 fresh issue/duplicate evidence 并重跑 implement route gate，获得 `allowed` 后才能开始 `SP165-T1`。本计划不把 auto 授权当作缺失证据或 gate-unavailable 专用授权。
- 覆盖全集为 `B-001`..`B-018`；`SP165-T1` 提供确定性 enforcement，`SP165-T2` 单独覆盖路由器前置分派 B-002，`SP165-T3`..`SP165-T9` 覆盖叶子直接调用 B-011，`SP165-T10` 收口分发兼容，`SP165-T11` 对全体行为做 exact-head 验证。
- 不得开始实现、提交、推送、评论、合并或发布；进入实现前仍需遵守 SpecRail readiness/spec approval、独立只读 review 与最终 PR gate。
