# Task Plan

## Linked Issue

GH-172

## Spec Packet

- Product: `product.md`
- Tech: `tech.md`

## 实现任务

- [ ] `SP172-T1` Owner: lock-contract | Depends on: approved spec | Done when: 单文件 fixture（`version: 1`）零改动通过，多文件 fixture（`version: 2`，并有 v1 reader 对其 fail closed 的回归）完整通过；v2 入口及每个 `files[]` item 都显式绑定 hash 与四字符 `mode` 闭集 `0644|0755`，source actual mode 必须相同，缺失/非法/危险 bits fail closed；shared manifest 从声明文件路径精确派生 strict parent `structural_directories`，`files[]` 允许目录成分但拒绝目录条目本身；`skills/*/SKILL.md` 的全部顶层分发 skill 都进入 completeness 集合，`implx` 与新增无前缀 fixture 均不能绕过 lock，任一集合/路径/哈希/mode 缺陷一次性报错 | Verify: `python3 -m pytest -q tests/test_evaluate.py -k "skills_lock or structural_directories or mode or unprefixed_skill"` | Covers: B-001 B-008 B-014 B-015 B-020 B-022 | 扩展共享 lock manifest：在 `checks/specrail_lib.py` 支持每个 skill 可选的目录内 `files[]` 闭集、显式 normalized mode 及不可由 caller 注入的 structural parents，保持现有 v1 单文件 `path`/`computedHash` 条目兼容；把顶层 discovery 从 `specrail-*` 改为所有直接子目录的普通 `SKILL.md`；拒绝未锁定 skill/file、重复、绝对/越界/反斜线路径、符号链接、非普通文件和 mode 不一致，并在 `tests/test_evaluate.py` 添加正反例。
- [ ] `SP172-T2` Owner: integrity-library | Depends on: SP172-T1 | Done when: 所有状态（含未声明文件导致的 `undeclared`/`invalid`）、混合缺陷、目标优先级、符号链接/逃逸、检查中变化、重复运行与无写入均有测试；nested locked file 的真实必要父目录被视为 structural entry 而通过，同级 stale/空目录仍为 undeclared；final component 以 `O_NOFOLLOW|O_NONBLOCK` 打开后立即 `fstat`，只有 `S_ISREG && st_nlink==1` 才能 hash，symlink/FIFO/special/hard-link 都在读前 fail closed，final nlink 仍须为 1；same-fd snapshot 同时验证 v2 exact normalized mode，`0755` 文件降为 `0644` 后即使 hash 相同也为 drift；namespace 只进入 derived structural directories，undeclared directory 作为 subtree root 不递归，固定 4096-entry global cap 命中后返回 exact=false/truncated=true/null total+omitted/空 sample；未命中 cap 时按 raw path bytes 排序，non-UTF-8 name 用 uppercase percent display；新增 integrity/CLI 总 branch coverage 至少 80%，其中 integrity library 为 100% | Verify: `python3 -m pip install --disable-pip-version-check coverage==7.15.2 && python3 -m coverage erase && python3 -m coverage run --branch --source=checks.installed_skill_integrity,tools.check_installed_codex_skills -m pytest -q tests/test_installed_skill_integrity.py && python3 -m coverage report --include='checks/installed_skill_integrity.py,tools/check_installed_codex_skills.py' --fail-under=80 && python3 -m coverage report --include='checks/installed_skill_integrity.py' --fail-under=100` | Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-016 B-017 B-018 B-019 B-020 B-022 B-026 B-027 B-028 | 新增 `checks/installed_skill_integrity.py` 与 `tests/test_installed_skill_integrity.py`：实现目标解析、structural-parent-aware bounded raw-byte namespace enumeration、逐锁定文件 nonblocking no-follow same-fd hash/mode/nlink snapshot、整体聚合、只读/有界输出和稳定排序；测试名包含 `nested_parent`、`fifo_swap`、`symlink_swap`、`hardlink`、`non_utf8`、`traversal_cap`、`mode_drift_same_hash` 与 `target_snapshot_no_write`，供 final gate 定点执行。
- [ ] `SP172-T3` Owner: doctor-cli | Depends on: SP172-T2 | Done when: 默认 `not_installed` 明确 skipped 且退出 0，`--require-installed` 仅在完整 match 时退出 0，普通 workflow check 不访问目标目录；只有显式 `--undeclared-artifact <new-file>` 才能导出同一次 bounded traversal 的 exact/truncated metadata、percent display 与 base64url raw paths，不能继续完整扫描；artifact parent 从 stable anchor 逐段 no-follow 打开并证明不在 target 内，最终以 parent dirfd `0600`/`O_EXCL|O_NOFOLLOW` create-only，parent symlink/rebind、覆盖、target 内 artifact 与 queue 使用均拒绝且 target/external sentinel 零写入；新增 CLI 在 branch coverage 模式下至少 80% | Verify: `python3 -m pip install --disable-pip-version-check coverage==7.15.2 && python3 -m coverage erase && python3 -m coverage run --branch --source=checks.installed_skill_integrity,tools.check_installed_codex_skills -m pytest -q tests/test_installed_skill_integrity.py tests/test_check_workflow.py -k "installed or required_files or undeclared_artifact or artifact_parent or non_utf8 or traversal_cap" && python3 -m coverage report --include='tools/check_installed_codex_skills.py' --fail-under=80` | Covers: B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-012 B-013 B-017 B-018 B-019 B-024 B-026 B-028 | 新增 `tools/check_installed_codex_skills.py`，提供 `--repo`、`--target-dir`、`--json`、`--require-installed`、`--undeclared-artifact`；将 checker/library 加入 `checks/check_workflow.py` 的 pack required files，但普通 workflow 主流程不得调用 installed inspect；补充 CLI、bounded-output、raw-byte artifact parent-swap 与 CI-no-home 测试。
- [ ] `SP172-T4` Owner: installer | Depends on: SP172-T1 SP172-T2 | Done when: dry-run 不写，已有 drift/missing/recovery-required 显式非零但打印完整计划；授权 apply 从 stable anchor 建立 target parent/root no-follow descriptor chain，所有 staging/exchange/rollback/recursive cleanup 只用 dirfd-relative 操作，commit 前后 rewalk 并验证 pathname 仍绑定同一 inode chain；parent/root 在 preflight 与 staging/commit 间被 symlink/rename/rebind 时 fail closed，external sentinel 零写入零删除；existing destination commit 使用 fsynced create-exclusive durable record + 单次 `RENAME_EXCHANGE`，canonical destination 在任意 kill/power-loss 点均存在，后续仅显式 apply 按 old/new manifest identity 确定 complete/rollback，矛盾现场保留并 fail closed；apply 只复制 manifest-declared files + derived structural directories，source 逐段 no-follow、final nonblocking open 后 immediate `S_ISREG && st_nlink==1` fstat，并从同一 fd 复制/哈希到 staging destination fd，再以 `fchmod` 保留 v2 exact mode，不受 umask 影响；source 在 validation/copy 间换成 hardlink/symlink/FIFO/special file 或发生 snapshot/hash/mode/nlink 变化时，在 exchange 前失败且不读取/落盘 escape 内容；post-check hash/mode/path binding 全 match 才清理 old tree 与 record | Verify: `python3 -m pytest -q tests/test_install_codex_skills.py -k "dry_run or apply or source_race or target_parent_swap or target_root_swap or hardlink or symlink or fifo or special or exchange or recovery or kill or power_loss or post_check or mode or umask"` | Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-014 B-015 B-016 B-017 B-019 B-020 B-021 B-022 B-025 B-027 | 重构 `tools/install_codex_skills.py` 使用 shared manifest/structural parents、目标解析和 pre/post inspect；移除会跟随 source race 的 whole-directory `copytree`、pathname-based target replacement 及 two-rename destination gap，以 stable target descriptor transaction + durable atomic exchange recovery + verified same-fd staging copy/mode/nlink 保留 dry-run 默认与显式 `--apply`；扩展 tests 覆盖多文件复制、source hardlink/symlink/FIFO race、target parent/root swap、exchange 前后 kill/power-loss recovery、mode/umask、apply 修复、post-check 失败、source-target 与 no-write。
- [ ] `SP172-T5` Owner: skill-integration | Depends on: SP172-T3 SP172-T4 | Done when: install/queue 两条路径消费同一 checker，且在 installed queue skill 之外增加源侧 bootstrap（`AGENTS.md` Long Queue Guardrails 与 `skills/specrail-workflow/SKILL.md` 路由器在委派给已安装 queue skill 之前先跑 `--require-installed` doctor）；queue 还必须消费 host/runtime-owned current-session `loaded_skill_evidence`，逐项绑定 session id、resolved installed path、loaded bytes sha256 与同一 lock manifest，拒绝 CLI/env/checkpoint/agent 自报。disk match + stale loaded bytes、apply 后同 session、provider unavailable 或任一 session/path/hash/manifest drift 都以 `session_restart_required` 在 lane/checkpoint/remote write 前阻断；只有新 session fresh matching evidence 才继续。不自动 apply/重启，不把 unavailable/drift 降级为 warning，lock 哈希与最终 skill 字节一致 | Verify: `python3 checks/check_workflow.py --repo . && python3 -m pytest -q tests/test_check_workflow.py -k "installed or loaded_skill or session_restart"` | Covers: B-003 B-010 B-011 B-012 B-013 B-018 B-020 B-023 | 更新 `AGENTS.md`、`AGENT_USAGE.md`、`CHANGELOG.md`、`skills/specrail-workflow/SKILL.md`、`skills/specrail-install/SKILL.md`、`skills/implx/SKILL.md` 与 `skills/specrail-implement-queue/SKILL.md`：安装 doctor 使用新 CLI，queue 在 lane/checkpoint/远端写入前同时要求 disk `--require-installed` match 与可信 loaded-session match；host evidence provider 是外部 availability prerequisite，缺失即阻断而非 disk-only fallback；同步三个修改后 installed skill 的 `computedHash`。

## 并行拆分

- 固定串行顺序 `SP172-T1 → SP172-T2 → SP172-T3/SP172-T4 → SP172-T5`。
- `SP172-T3` 与 `SP172-T4` 在 T1/T2 接口冻结后可并行：T3 独占
  `tools/check_installed_codex_skills.py`、`checks/check_workflow.py`、
  `tests/test_check_workflow.py`；T4 独占 installer 与其测试。
- `SP172-T5` 必须最后串行执行，因为三个 Skill 与 `skills-lock.json` 是共享收口面。
- 任一并行 lane 不得修改另一个 lane 的 writable files；集成 owner 负责最终哈希。

## 验证

- [ ] `SP172-T6` Owner: verification-owner | Depends on: SP172-T1 SP172-T2 SP172-T3 SP172-T4 SP172-T5 | Done when: 所有命令本轮全绿；T2 combined/library coverage 与 T3 CLI-only coverage 各自执行并达到 80%/100%/80% 强制阈值；`target_snapshot_no_write` 明确证明 doctor 前后路径/type/mode/mtime_ns/hash 闭合 snapshot 相同；定点负例覆盖 stale loaded session/apply-without-restart、artifact parent swap、exchange 前后 kill recovery、non-UTF-8 name、external hard link 和 traversal cap，全部在各自安全边界 fail closed；manifest 为 17 个 unique paths 且不含 GH-160，PR diff 也无 `specs/GH160`；manifest 中所有新增/修改 Python 与 Skill 文件严格 `<800` 行 | Verify: `python3 -m pip install --disable-pip-version-check coverage==7.15.2 && python3 -m coverage erase && python3 -m coverage run --branch --source=checks.installed_skill_integrity,tools.check_installed_codex_skills -m pytest -q tests/test_installed_skill_integrity.py && python3 -m coverage report --include='checks/installed_skill_integrity.py,tools/check_installed_codex_skills.py' --fail-under=80 && python3 -m coverage report --include='checks/installed_skill_integrity.py' --fail-under=100 && python3 -m coverage erase && python3 -m coverage run --branch --source=checks.installed_skill_integrity,tools.check_installed_codex_skills -m pytest -q tests/test_installed_skill_integrity.py tests/test_check_workflow.py -k "installed or required_files or undeclared_artifact or artifact_parent or loaded_skill or session_restart" && python3 -m coverage report --include='tools/check_installed_codex_skills.py' --fail-under=80 && python3 -m pytest -q tests/test_installed_skill_integrity.py -k "nested_parent or fifo_swap or symlink_swap or hardlink or non_utf8 or traversal_cap or artifact_parent or target_snapshot_no_write" && python3 -m pytest -q tests/test_install_codex_skills.py -k "hardlink or exchange or recovery or kill or power_loss" && python3 -m pytest -q tests/test_check_workflow.py -k "loaded_skill or session_restart" && python3 -m pytest -q tests/test_installed_skill_integrity.py tests/test_install_codex_skills.py tests/test_evaluate.py tests/test_check_workflow.py && python3 -m pytest -q && python3 checks/check_workflow.py --repo . --all-specs && python3 tools/spec_depth_audit.py --spec-dir specs/GH172 --gate && sed -n '9p' specs/GH172/tech.md | jq -e '(.paths|length)==17 and (.paths|unique|length)==17 and ([.paths[]|select(startswith("specs/GH160/"))]|length)==0' && test -z "$(git diff --name-only "$(git merge-base HEAD origin/main)"..HEAD -- specs/GH160)" && for path in $(sed -n '9p' specs/GH172/tech.md | jq -r '.paths[]|select(endswith(".py") or endswith("/SKILL.md"))'); do test "$(wc -l < "$path")" -lt 800 || exit 1; done && git diff --check` | Covers: B-001 B-002 B-003 B-004 B-005 B-006 B-007 B-008 B-009 B-010 B-011 B-012 B-013 B-014 B-015 B-016 B-017 B-018 B-019 B-020 B-021 B-022 B-023 B-024 B-025 B-026 B-027 B-028 | exact-head final gate 不得以 library coverage 替代 CLI gate，也不得以普通 tests 隐含 loaded-session/no-write/interruption-recovery/scope/line-count 断言。

## Handoff Notes

- 当前 issue 只有 `ready_to_spec`；本 spec PR 合并并由维护者切换到
  `ready_to_implement` 前，不得执行以上实现任务。
- 实现 manifest 严格限定 tech spec 声明的 17 个路径；不得修改 GH-160、自动安装本机
  skill、自动重启会话、创建标签或把 doctor 结果写成 checkpoint。测试可使用 fake
  runtime-owned loaded evidence；真实 host provider 未部署时 queue 必须保持
  `session_restart_required`/unavailable。
- GH-160 与 GH-174 都依赖本 issue 的多文件 lock/installer/doctor 合同；GH-172 合并前
  两者均不得修改 `skills/specrail-implement-queue` 或 `skills-lock.json`。二者各自实现
  reference 时必须声明该 skill 的完整 `files[]`、入口和每个文件的 exact mode，并迁移为
  v2 lock。
- 本 issue 验证“磁盘 Skill 资产是否匹配”并规定 current-session loaded identity 的 fail-closed
  queue contract；host/runtime evidence provider 是外部 prerequisite。把 runtime
  gate/checker/provider 作为全局可执行依赖分发仍是后续独立 issue，不得在实现时偷偷扩大范围。
