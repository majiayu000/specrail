interface:
  display_name: "Implx"
  short_description: "Explicit SpecRail issue and pull request queue runner."
  default_prompt: "Use $implx for this queue."
policy:
  allow_implicit_invocation: false
